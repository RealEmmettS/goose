# honk300 v1.2.6 Release Readiness

Date opened: 2026-07-18

Target: stable/latest `v1.2.6`

Release task: `#cli123`

Architecture decisions: ADRs 0031 and 0034–0035

## Intent

v1.2.6 is the immutable Windows compatibility fix-forward after both public v1.2.5 Windows
lanes proved that supported system Windows PowerShell can lack the optional `Get-FileHash`
cmdlet. The public bootstrap had already downloaded and same-stream-verified exact MSI and
lifecycle bytes, acquired its lease, and completed the MSI before that later launcher-receipt
hash failed. Both bootstrap and CLI coordinator now use direct .NET file streams and SHA-256 for
every remaining hash. Regular-file, reparse-point, size, digest, lifecycle, and final receipt
verification remain fail closed.

## Source and local gate

- [x] Preserve immutable v1.2.5 and record its complete candidate/main/publication evidence plus
  the six passing and two failing public-byte lanes.
- [x] Remove `Get-FileHash` from the public bootstrap and Windows update coordinator.
- [x] Add focused contracts requiring direct .NET SHA-256 and forbidding the optional cmdlet.
- [x] Pass format, strict workspace/cross-target gates, all Rust/Python contracts, locked build,
  RustSec, actionlint, cargo-dist v1.2.6 plan, and clean package-list gate.
- [x] Keep local verification noninteractive; use disposable Windows CI for installer mutation.

## Candidate, main, publication, and public bytes

- [x] Pass the exact v1.2.6 candidate across all platform producers and final assembly.
- [x] Merge the unchanged candidate SHA to main and pass ordinary same-SHA CI.
- [x] Create immutable v1.2.6, publish all 47 assets atomically, and verify manifest identity.
- [x] Pass all eight fresh-public-byte jobs, including PowerShell bootstrap takeover on x64/ARM64.
- [x] Verify exact-tag/latest identity and the live command-first production website.

## Closure

- [x] Record exact run IDs, commit, public hashes, latest aliases, and site evidence.
- [x] Complete `#cli123`, retain only clean main, and remove every temporary local/remote branch
  after proving its tip is merged or deliberately superseded.

## Evidence log

- 2026-07-18 — v1.2.5 public run `29663081572` passed both Mac, both Linux shell, and both Debian
  lanes. Captured Windows x64/ARM64 stderr identified `Get-FileHash` as unavailable only after the
  bootstrap MSI completed, converting the prior generic exit into a precise compatibility fix.
- 2026-07-18 — complete noninteractive v1.2.6 local qualification passed: format/diff hygiene,
  strict native and both-musl Clippy, ARM64 Windows check, all 434 Rust tests, locked optimized
  build, all 120 Python contracts with three honest host skips, PowerShell and Git-Bash parsers,
  actionlint, cargo-dist planning, RustSec over 410 dependencies, and the 104-file package shape.
  No Goose, installer, UAC prompt, or visible helper ran locally.
- 2026-07-18 — exact candidate `29663447227` and ordinary same-SHA main CI `29663774278` passed
  at `c0ddb2c2c7cd4334040e324c72187eb6f3d4a644`. The matrices covered both Apple architectures,
  x64 and native ARM64 Windows slot/install/takeover/rollback/compositor behavior, and all released
  Linux architectures without changing the candidate bytes.
- 2026-07-18 — atomic release `29664104522` published v1.2.6 from that exact commit after every
  producer, native package lifecycle, signed/notarized/stapled Mac app/DMG, native ARM64 Windows
  lifecycle, and final remote-byte verification passed. The immutable release contains 47 assets
  and 22 manifest payloads.
- 2026-07-18 — an independent fresh download matched exact-tag and rolling-latest bytes. SHA-256:
  `release-manifest.json` `3345862c98bb043d3d6940ab9f82822defb22147af874bdb320b3bdcde95beb9`;
  `honk300-installer.ps1` `868466b1e796261f3d546f0ea7ce6293a0380f7965f927b5a146797f4ff8d4e5`;
  `honk300-installer.sh` `6db94c80a4bc65dbe693be9081d2db304b2e7f24359a617511751f7c7663cc0a`.
  The manifest identifies v1.2.6 and the exact source commit above.
- 2026-07-18 — fresh-public-byte run `29664433689` passed all eight jobs: signed managed Mac on
  Intel and Apple Silicon, shell-managed Linux and native Debian on amd64/arm64, and actual public
  PowerShell-bootstrap/Global-MSI takeover, rollback, repair, PE, compositor, lifecycle, and
  uninstall qualification on Windows x64 and native ARM64.
- 2026-07-18 — production browser QA at `https://thegoose.app/` resolved verified v1.2.6, showed
  the versionless IRM command first on Windows and the versionless shell command first on Mac/Linux,
  retained DMG/MSI/EXE/DEB/portable alternatives, switched all platform views correctly, emitted
  no warning/error console messages, and had no horizontal overflow at a 390x844 mobile viewport.
- 2026-07-18 — every temporary Goose and website release branch was proven contained in main and
  removed locally/remotely. Both worktrees are on clean, synchronized main; the site's unrelated
  open ImgBot PR branch remains untouched because it is neither merged nor part of this release.
