# Honk300 v1.4.0 release readiness

Opened 2026-09-07. Task #rr1; refinement stage one. Decisions: ADR 0041 and ADR 0042.

This candidate adds the complete continuous goose redesign, expressive gait/reactions,
shared runtime reliability corrections, and the Native SDK settings companion with
shared Rust persistence, native accessibility and existing updater/lifecycle ownership.
Linux owned props, experimental Pi qualification and opt-in Wayland adapters remain
the following staged releases in the approved refinement milestone.

Public stable remains v1.3.7. No v1.4.0 tag or release has been created. All existing
tags and assets are immutable. The user's existing Windows installation remains untouched.

## Required gates

- [x] Review actual final heading/motion exports and preserve planted-contact/alpha/bounds checks.
- [x] Record baseline/new renderer workloads and distinguish CPU rendering from desktop/Pi performance.
- [x] Exercise isolated native Windows settings edit/save and update discovery plus the TUI check action.
- [x] Pass the complete local gate on the final candidate tree.
- [ ] Pass all eight native settings lanes, including actual Windows UIA and GNU Linux AT-SPI actions.
- [ ] Pass the candidate CI, native Windows installer matrix and signed/notarized/stapled Mac production.
- [ ] Merge the qualified source and pass unchanged-source main gates.
- [ ] Publish the new immutable tag and complete platform set atomically.
- [ ] Verify all eight lanes from fresh public bytes and coordinate the website release status.

## Evidence and limits

The reviewed source `e7f37e277353e6e1746298dbfa9134f26b7fff7a` passed CI 34190236003,
all eight native settings lanes 34190235891 and complete candidate 34190232598, then
merged through PR #9. Before tagging, the Linux follow-on review identified two shared
companion verification defects: an opened executable was launched through its pathname,
and invalid receipts could fall through to unmanaged acceptance. Both affect the GUI
launcher in this stage as well. Publication is held for descriptor-bound Linux exec,
strict receipt evidence and their regression/native gates. No existing release changed.

PR #7 merged at `972e6b4cb9fcfc55dcc864243c14f84adcc199e2` after full CI 34184307688,
all eight native settings lanes 34184307500 and complete candidate 34184304491 passed.
Before tagging, its final review identified four confirmed edge cases: GUI read/save could
promote stale autostart intent, runtime-status failure blocked editing, a missing reload
file selected defaults, and a non-Unicode path panicked during unused JSON serialization.
The corrected source must repeat candidate and main gates. Its local regressions reproduce
the missing-file/path failures before correction and pass afterward. An isolated native
Windows test refutes the separate null-standard-handle report: windows 0.58 already maps
NULL and INVALID_HANDLE_VALUE to Err, which the launcher skips. Valid handle inheritance
is still cleared and tested. The earlier green candidate is not the final release source.

The complete candidate at `e51b76f6ee19d41110243475914bb11c71ebec2c`
passed [run 34181368273](https://github.com/RealEmmettS/goose/actions/runs/34181368273):
all eight native settings builds and portable targets, both Windows installer editions
and architectures, native Windows compositor/package qualification, signed/notarized/stapled
universal Mac app and DMG, both Debian install/update/remove/purge lanes, and the complete
verified candidate artifact set. This was a candidate build and published nothing.

Source `6ed766669da5ac544cd931320cb410ee5f94b6ba` then passed the genuine Debian
CLI/TUI/GUI update transaction gate, including offline GUI failure, an unchanged failure
receipt, a new check followed by retry, runtime relaunch and public no-op. The fixture now
waits for the asynchronous check response before invoking Update again. Full CI
34182815944 and all eight native settings lanes in 34182815747 passed. Its additional
candidate repeat is recorded in 34182828478.

The follow-on Linux integration work added strict native Clippy and found a stale glob
import in the earlier installer extraction. It had no production callers on Linux; the
public re-exports are unchanged. The final source limits that import to Windows/tests
and adds strict Clippy to both Linux CI architectures. Final source/main/public-byte
qualification remains required after this correction.

The complete Windows-host Rust workspace suite, strict Clippy, release build, 130 Python
checks (three platform skips), native settings tests/check/production build, AccessKit
translation/consumer tests and dual-file-lease tests passed. The native settings fixtures
exercise all five pages, eight simultaneous changed fields on the largest page, actual
Windows UIA and GNU Linux AT-SPI actions/text/modal isolation, and GUI Start/Stop. Release
artifacts bind the exact GUI and Windows accessibility DLL to their manifest and receipt.

Actual Windows isolated settings edit/save, native text/focus/status readback, CLI/TUI/GUI
update discovery, captured start EOF and native GUI Start/Stop were observed. Closing and
reopening settings preserved the independently running runtime. Final light/dark heading
and animation exports were inspected and delivered to the separate website task.

The audit and before/after CPU, memory and IPC evidence are in
[the refinement audit](../refinement-audit.md) and
[the measurement record](refinement-render-measurements.md). Measurements distinguish
renderer cost from the actual single-display Windows workload and do not claim physical
Pi performance or an unlimited-duration memory plateau.

Earlier failures are retained in their workflow evidence and task activity: WiX notice
component identities, omitted ARM64 qualification payloads, asynchronous GUI fixture
reads, TUI terminal setup/teardown, Linux disabled-button accessibility state, and a
capture that interrupted the startup entrance. The corresponding production or fixture
corrections preserve the existing installer, lifecycle and image oracles.

Physical Mac/Pi equipment is unavailable, and these checks do not claim its acceptance.
The historical Windows v1.3.5 installation and unapproved v1.3.7 elevation remain open
in the existing readiness record; this release does not silently close those tasks.
