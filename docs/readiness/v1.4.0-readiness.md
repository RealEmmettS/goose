# Honk300 v1.4.0 release readiness

Opened 2026-09-07. Task #rr1; refinement stage one. Decisions: ADR 0041 and ADR 0042.

This candidate adds the complete continuous goose redesign, expressive gait/reactions,
shared runtime reliability corrections, and the Native SDK settings companion with
shared Rust persistence, native accessibility and existing updater/lifecycle ownership.
Linux owned props, experimental Pi qualification and opt-in Wayland adapters remain
the following staged releases in the approved refinement milestone.

Public stable remains v1.3.7. No v1.4.0 tag or release has been created. All existing
tags and assets are immutable. The user's existing Windows installation remains untouched.

## Required gates

- [x] Review actual final heading/motion exports and preserve planted-contact/alpha/bounds checks.
- [x] Record baseline/new renderer workloads and distinguish CPU rendering from desktop/Pi performance.
- [x] Exercise isolated native Windows settings edit/save and update discovery plus the TUI check action.
- [ ] Pass the complete local gate on the final candidate tree.
- [ ] Pass all eight native settings lanes, including actual Windows UIA and GNU Linux AT-SPI actions.
- [ ] Pass the candidate CI, native Windows installer matrix and signed/notarized/stapled Mac production.
- [ ] Merge the qualified source and pass unchanged-source main gates.
- [ ] Publish the new immutable tag and complete platform set atomically.
- [ ] Verify all eight lanes from fresh public bytes and coordinate the website release status.

## Evidence and limits

The audit and CPU measurements are recorded in `docs/refinement-audit.md` and
`docs/readiness/refinement-render-measurements.md`. The stamped Windows-host Rust
workspace tests, strict Clippy and release build passed, as did 130 Python checks
with three platform skips. Native settings tests/check/production build, the
AccessKit consumer and file-lease tests passed on Windows. The final settings
smoke includes the largest changed page and live public update discovery. Cargo-dist
planned all eight v1.4.0 targets; the separate accessibility lockfile audit passed.

Before stamping, exact source `8a3087540013fb42aa9114c88c58bdc50879d6b0` passed
all eight Native settings lanes in run `34173523099` and the full CI run
`34173523312`, including both Windows native overlay gates and actual Windows
UIA/GNU Linux AT-SPI edit/save. The stamped source and added entrypoint/lifecycle
fixtures must repeat the gates.

Actual Windows UI Automation exposes the changed-page Save button, modal edit field,
text and saved status; mouse/keyboard changes persisted to an isolated config. The
command line, real terminal editor and native GUI all discovered public v1.3.7 and
correctly kept the unmanaged development build ineligible for automatic installation.

Hosted Linux runtime checks already exercise a genuine older Debian fixture updating
through the production retained helper, exact package ownership, receipt readback and
runtime relaunch. The final same-source/candidate/public-byte repeats remain required.

Physical Mac/Pi equipment is unavailable, and these checks do not claim its acceptance.
The historical Windows v1.3.5 installation and unapproved v1.3.7 elevation remain open
in the existing readiness record; this candidate does not silently close those tasks.
