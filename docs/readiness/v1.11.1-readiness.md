# Goose v1.11.1 readiness

## Scope

The settings and tray follow-up in [ADR 0054](../adr/0054-settings-navigation-and-shared-artwork.md)
and task #g12: visible selected tabs, short smooth wheel scrolling, Linux-only Wayland
controls, and each platform's existing application artwork in its tray or menu.
The approved Windows application badge and Mac/Linux application PNG are unchanged.

## Local qualification

The installed v1.11.0 Global MSI is verified at source
`630f754a0ef657172b0a6a7edea65593fa514de5`, with matching protected launcher and settings
payload hashes. The user confirms the logo and graphical controls now work. A live
installed window reproduces the indistinguishable tab and one-notch runaway scroll.

The rebuilt v1.11.1 native window, using an isolated configuration, shows persistent
selection on all five pages. One wheel notch advances a small distance and remains
settled; a reverse notch returns to the top. Windows does not show the Wayland control
even with `platform.wayland = true` retained in that isolated file. The real user
configuration is not used for those edits. Native screenshots and accessibility trees
are inspected directly during the test.

Production SDK physics checks cover 30/60/120 Hz settling, repeat/reverse input,
boundaries and reduced motion. Rust service checks prove platform visibility and
retention of a hidden saved Wayland value. Linux's private D-Bus regression verifies
the actual exported icon bytes, including straight alpha and ARGB channel order.
Mac AppKit tests decode the exact application source as an 18-point template, and
packaging compares the sealed status PNG with its application source before publishing.

Local logs and state-preservation evidence are in `target/settings-polish-review/`.

## Release gates

- [x] Required local Rust/Python and Native SDK tests, strict checks and real builds.
- [x] Native Windows lifecycle/tray registration and restoration of the installed runtime.
- [ ] Exact-source standalone qualification on every required platform.
- [ ] Full candidate: native Windows installers, both Debian packages, fresh signed,
  notarized and stapled universal Mac app and DMG.
- [ ] Unchanged-source main qualification.
- [ ] Atomic immutable publication of the complete artifact set.
- [ ] Fresh-public qualification and independent exact-byte download verification.
- [ ] Matching website release guidance.

No public tag or publication is claimed by this initial record. Physical Mac permission
and screen-reader acceptance remain separate from hosted checks.

The local lifecycle test passes against release-binary SHA-256
`cddddf07a8fde851c69142088a5240540c70748f042f3ade97ea5608702335a5`.
It proves actual tray registration and recovery, all command aliases, graceful native
Quit and force behavior. The installed v1.11.0 runtime is restored afterward, and the
user configuration hash remains `817ae495bc300db3d42a9b1e05780d974b4eb8052d2ec5a258b835b742e44bb4`.
The isolated GUI configuration also remains byte-for-byte unchanged.
