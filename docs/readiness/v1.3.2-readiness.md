# honk300 v1.3.2 Release Readiness

Date opened: 2026-07-19

Target: stable/latest `v1.3.2`

Release task: `#r132`

Existing decisions: ADRs 0034 and 0036

## Intent

v1.3.2 makes the public Windows bootstrap independent of processor architecture variables inherited
from the launching terminal. It keeps the existing WOW/process probes, then falls back to the
Windows machine value and .NET OS architecture. Artifact selection, exact-tag size/SHA-256 checks,
Global-MSI/UAC ownership, slots, receipts, aliases, and detached app runtime do not change.

## Source and local gate

- [x] Reproduce the exact v1.3.1 public command failure in the Codex terminal before download/UAC.
- [x] Prove the installed v1.2.6 receipt, timestamp, version, and files remain unchanged.
- [x] Add ordered machine/runtime fallbacks and a scrubbed-environment template execution test.
- [x] Pass formatting, strict workspace Clippy, locked tests, x64/ARM64 checks, Python contracts,
  PowerShell parsing, actionlint, RustSec, cargo-dist v1.3.2 planning, and package shape.
- [x] Pass the clean committed-tree package-list gate for the exact candidate bytes.

## Candidate, main, publication, and public bytes

- [x] Pass the exact v1.3.2 candidate across all platform producers and final assembly.
- [x] Fast-forward the unchanged candidate commit to main and pass ordinary same-SHA CI.
- [x] Create immutable v1.3.2, publish the complete asset set atomically, and verify manifest
  identity against the exact tag and commit.
- [x] Pass all eight fresh-public-byte jobs for Windows x64/ARM64, Mac Intel/Apple Silicon, Linux
  shell x64/ARM64, and Debian amd64/arm64.
- [x] Verify exact-tag/latest byte identity and the live command-first production website.

## Alienware installation

- [x] Install official v1.3.2 through the unmodified versionless public command from the Codex app
  terminal with both process architecture variables absent.
- [x] Prove version, protected receipt origin/scope/target/artifact, aliases, PATH/registration,
  public-byte identity, launcher hash, no duplicates or staging residue, and one-object updater JSON.
- [~] Prove typed start returns while one hidden runtime/tray remains across a duplicate start, then
  stop gracefully and leave no Honk300 process.
  > Controller/app readiness and exact runtime identity passed, but Codex's surrounding Windows job
  > tracked the runtime because a new process group is not job detachment. Strict renderer/DWM
  > evidence stayed transparent. Immutable fix-forward task `#r133` owns the breakaway repair.

## Closure

- [x] Record exact run IDs, commit, asset count, public hashes, installation evidence, and site
  evidence.
- [x] Complete `#r132` and transfer grouped release-branch cleanup to `#r133` after the immutable
  job-breakaway fix-forward closes.

## Evidence log

- 2026-07-19 — v1.3.1 public command failed before mutation in the Codex terminal because inherited
  processor variables were absent. Machine scope reports `AMD64`; .NET reports `X64`. The existing
  protected receipt remained SHA-256
  `8142140243AB38F1A590DB138F589A9CF94A0E591021737F0570CE36E205F9F1` at
  `2026-07-19T06:20:39.7232925Z`, and all aliases still reported 1.2.6.
- 2026-07-19 — the repaired template prefix executed with both process variables removed, resolved
  `x86_64-pc-windows-msvc`, and passed its focused Python execution plus PowerShell parser tests.
- 2026-07-19 — complete local qualification passed: formatting, strict native and both musl Clippy
  targets, 445 locked Rust tests, optimized x64 PE/subsystem checks, ARM64 Windows release compile,
  123 Python contracts with three platform skips, every PowerShell parser, actionlint, RustSec over
  418 locked dependencies, eight cargo-dist targets, and 108 packaged files.
- 2026-07-19 — the bounded physical lifecycle passed in 79.8 seconds without desktop obscuration.
  Candidate build SHA-256 `631E80243A9930B8DA1D6CF41B54E829324EF1D6EE48767628BE1D09974A38B1`
  passed transient controller/app detachment, singleton/tray retention across repeated starts,
  graceful/forced command matrices, immediate restart recovery, and left no Honk300 process.
- 2026-07-19 — clean committed source `7c068ac419c44b66e453e18027acaf0020d7487e` passed the
  108-file package-list gate with the bootstrap template/regression, console and GUI Windows
  entrypoints, and version 1.3.2 metadata present.
- 2026-07-19 — exact final commit `656b9295f095df486255f80854a63a77a5f31e6c` passed candidate
  `29708871482` (19/19) and same-SHA main CI `29709167422` (12/12). Publication run
  `29709410451` succeeded on attempt 3 after two official GitHub Actions/API outage failures, and
  fresh-public-byte run `29711678501` passed all eight lanes. The release is immutable, targets the
  exact commit, and contains 47 assets with 22 manifest payloads.
- 2026-07-19 — exact/latest public bytes agree: manifest 7,670 bytes at SHA-256
  `d9b886bcda5ceabbe328feeae205736e9c3a71a4adb36aed2c22c3daa317692c`, PowerShell
  bootstrap 13,836 bytes at `244e9d16b024276288df22956ead8e3d84572529bcf6d0f6dc2790ca4f2cab81`,
  and shell bootstrap 26,244 bytes at
  `45ce5943037ee533cafef7bb409642af34038a1b91fe04980998656301fb116d`. Production
  desktop and 390x844 mobile served v1.3.2, correct versionless commands/release links, no browser
  errors, and no horizontal overflow.
- 2026-07-19 — the official command installed v1.3.2 machine-wide from the scrubbed Codex terminal.
  The protected receipt SHA-256 is
  `FE717458F11AF51B94FCF7DB8D13815077D25E8D7148C044540C700E02A34CA8`; it records
  PowerShell/Global/machine ownership, exact target and commit, and MSI SHA-256
  `d2298c41c3691effff664736805691afa30747b6b931d1e1059ba01b25c9c663` at 19,427,328
  bytes, matching the public manifest. All aliases report 1.3.2 with identical hash
  `3DFF1B6274C1DBD6147D0C0EA64BE6FD24739B9489D0DA981B1A9E9A19D9C4D4`; launcher
  hash `6D38529C67055910D69F7EB06D899B7743EDAC63B8A8F61CB5A830A2B033D06F` matches its
  receipt, and one HKLM Global MSI registration exists.
- 2026-07-19 — installed `goose update --json` exited zero with exactly one stdout JSON object,
  `result: up_to_date`, version 1.3.2, origin `powershell`, unchanged activation, and clean cleanup.
  Progress stayed on stderr, and both protected/discovery receipt hashes and timestamps were
  unchanged. This conclusively closes the original permission-denied updater failure.
- 2026-07-19 — final Codex lifecycle qualification exposed the distinct job-containment gap.
  Raw public presenter evidence had 14,229 transparent pixels and zero opaque-black pixels;
  strict `CAPTUREBLT` evidence kept v1.2.6 direct, v1.3.2 direct, app-launched, and graceful-stop
  frames transparent. The pasted black rectangles came only from a screen-copy path omitting the
  required layered-window flag. The ready runtime nevertheless kept Codex's surrounding job alive;
  ADR 0037 and `#r133` own the immutable breakaway fix.
