# honk300 v1.3.3 Release Readiness

Date opened: 2026-07-19

Target: stable/latest `v1.3.3`

Release task: `#r133`

Decision: ADR 0037

## Intent

v1.3.3 lets the transient Windows app launcher break its exact hidden runtime out of a
breakaway-permitted integrated-terminal job. It keeps the original windowless flags as the only
access-denied fallback and does not change renderer, runtime, installer, updater, or ownership
behavior.

## Source and local gate

- [x] Prove v1.3.2's controller/app/runtime readiness succeeds but the Codex command job remains
  retained because `CREATE_NEW_PROCESS_GROUP` does not detach jobs.
- [x] Prove the reported black clipboard rectangle is absent from the raw presenter and strict
  DWM `CAPTUREBLT` path through ordinary and graceful app-launched frames.
- [x] Add breakaway-first, access-denied-only fallback creation plus focused Rust/packaging tests.
- [x] Prove an optimized build returns the exact Codex origin shell/tool call while one hidden
  runtime remains independently alive and status-responsive, then force-stop with no residue.
- [x] Pass formatting, strict workspace Clippy, locked tests, x64/ARM64 checks, Python contracts,
  PowerShell parsing, actionlint, RustSec, cargo-dist v1.3.3 planning, and package shape.
- [ ] Pass the clean committed-tree package-list gate for the exact candidate bytes.

## Candidate, main, publication, and public bytes

- [ ] Pass the exact v1.3.3 candidate across all platform producers and final assembly.
- [ ] Fast-forward the unchanged candidate commit to main and pass ordinary same-SHA CI.
- [ ] Create immutable v1.3.3, publish the complete asset set atomically, and verify manifest
  identity against the exact tag and commit.
- [ ] Pass all eight fresh-public-byte jobs for Windows x64/ARM64, Mac Intel/Apple Silicon, Linux
  shell x64/ARM64, and Debian amd64/arm64.
- [ ] Verify exact-tag/latest byte identity and the live command-first production website.

## Alienware installation

- [ ] Install official v1.3.3 through the unmodified versionless public command.
- [ ] Prove protected/discovery receipt identity, origin/scope/target/artifact, aliases,
  PATH/registration, public-byte identity, launcher hash, and one-object updater JSON.
- [ ] Prove the Codex origin command and tool call return while exactly one transparent hidden
  runtime/tray remains across duplicate start, then stop gracefully and leave no process.

## Closure

- [ ] Record exact run IDs, commit, asset count, public hashes, installation evidence, and site
  evidence.
- [ ] Complete `#r133`, retain clean synchronized main, and remove grouped merged release branches.

## Evidence log

- 2026-07-19 — the Codex shell is inside a Windows job with limit flags `0x800`
  (`JOB_OBJECT_LIMIT_BREAKAWAY_OK`). v1.3.2's public start created a ready hidden runtime but the
  tool cell remained retained until force stop because its launcher requested only a new process
  group.
- 2026-07-19 — public-launch presenter evidence recorded 14,229 fully transparent pixels, zero
  opaque-black pixels, and valid premultiplied channels. Strict `CAPTUREBLT` captures kept the
  desktop visible under v1.2.6 direct, v1.3.2 direct, v1.3.2 app-launched, and every captured
  graceful-stop frame. A non-layered-aware screen copy alone reproduced the pasted black box.
- 2026-07-19 — complete local qualification passed: formatting/diff checks, 123 Python release
  contracts with three platform skips, five PowerShell parsers, actionlint, 446 locked Rust tests,
  strict native plus x64/ARM64 musl Clippy, optimized x64 console/GUI PE proof, ARM64 Windows
  release-mode compile check, RustSec over 418 dependencies, an eight-target cargo-dist v1.3.3
  plan, and 108 packaged files.
- 2026-07-19 — final local binary SHA-256
  `325E2E5A6B07C09E543C2BAB9D109C7FF950F7370D5D089ED4013960232F5660` and launcher SHA-256
  `22838ECF75AAB5B7CEFEBE14538A917F1377C56AF0CA2EE5389E0D4F7B97DD5A` passed the complete
  81.1-second visible-lifecycle-no-obscuration matrix. A separate exact Codex command returned
  while runtime PID 36940 stayed status-responsive; duplicate start preserved it, graceful stop
  completed in 3.484 seconds, and zero product processes remained.
