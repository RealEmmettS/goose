# v1.11.0 readiness

## Scope

One combined Goose desktop update, as requested in [ADR 0052](../adr/0052-goose-application-and-affection.md): reliable installed graphical controls, visible application icons and names, application-first activation, recognizable temporary leaves, and affectionate following after sustained rubbing. Existing backend, installer ownership, runtime lifecycle, autostart, compatible command aliases and strong charging scatter remain intact.

## Local evidence

- Reproduced the user's installed v1.10.1 Configure failure: a protected Global MSI receipt conflicted with a stale per-user PowerShell v1.3.5 record for the same root. The new regression reproduces that failure, then passes with owned-receipt precedence; malformed and tampered owned evidence remain rejected.
- Preserved the stale record under `install-receipt.stale-v1.3.5-20260909.json`, then exercised actual installed tray Configure and Update. Configure opened the verified companion; the real Update console reported the already-current release. Actual GUI Start and graceful Stop passed. Existing installed payload and protected receipt remain v1.10.1.
- The only saved preference change enabled requested petting feedback through a revision-checked Rust settings save. Login startup and unrelated preferences remain unchanged.
- Rust formatting, strict workspace lint, all workspace tests and the release build pass. All 141 Python packaging checks pass with three platform skips. Native SDK tests, checking, production build and eight independent Windows OS accessibility checks pass.
- Inspected actual engine-rendered leaf and affection contact sheets. Untouched piles visibly fade to empty; running scatter retains its velocity-dependent force. Timed tests verify rubbing duration, hearts, walking speed, separation, expiry, cancellation and no cursor/window commands.
- Staged Windows settings, bare-command and app-menu routes open a Goose window with a nonzero native class icon, loaded OS-accessible controls and a returning launcher. All three executable images contain an icon. The staged check uses an isolated profile: an unpackaged development process correctly refuses to own the user's enabled managed autostart setting.
- The user's follow-up screenshots show a generic taskbar icon and a Search result classified as Run command. Live readback confirms the still-open installed v1.10.1 settings window has zero large/small window and class icons, and Windows has no Goose entry in its application index. These remain installed acceptance checks for the new version; development results do not replace them.
- The final SVG-enabled release binary passes the welcomed Windows lifecycle-only smoke, including controller detachment, graceful entry/exit, singleton behavior, reload and tray recovery. This run performs no full-desktop calibration.

Local evidence is in `target/desktop-polish-review/`. Production installed regression coverage verifies every Windows installer origin, both native architectures, the actual app-menu target, installed-app name/icon, GUI identity and a deliberately stale external receipt on disposable CI hosts.

The first hosted source run, CI 34316797228 at `250c525610b0facd7a476cbad81474b8bde0deda`, found that the Linux private-D-Bus test still searched for the old Configure/Update/Quit labels on both architectures. Correct the exact expected names and remaining accessible tray descriptions; preserve action dispatch, icon contents and watcher-loss/recovery assertions. Superseded qualification is not publication evidence.

Candidate 34317313173 at `ef7a81c3589cef9608423ee619ee1844e47ef9a4` exposed the Global MSI's undefined shortcut-directory property. Its native transaction log shows `CommonProgramsFolder = D:\` and a shortcut at `D:\Goose\Goose.lnk`, while standard `ProgramMenuFolder` correctly resolves to the all-users Start menu. The user's misplaced old shortcut reproduces the original issue. Correct the installer property and the old static assertion that had pinned the invalid property; retain the actual installed-link regression. This candidate also passed every Mac presence test before an artifact-upload DNS failure (`ENOTFOUND`); that failed upload does not count as completed qualification.

Candidate 34318662398 at `a052f39ff934ba1579e14db0b94a8359d0c8228e` caught ICE43/ICE57 when the HKLM shortcut component directly used the contextual standard directory. Bind that standard path to an explicit machine-owned directory before costing instead. Local WiX compilation and native linking pass without suppressing either validation rule. A Windows Installer cost-only session resolves the real all-users Goose folder and leaves product state uninstalled; it does not execute installation or replace the user's current package. Final native installation checks remain required on the corrected source.

## Publication gates

Candidate 34320068608 at `fbfb5990a3d5c4f8101b310e58df6bddd0070190` successfully installed the Global MSI's real all-users shortcut, then exposed a new fixture's incorrect requirement for an MSI `DisplayIcon` registry value. A read-only local probe of installed Microsoft products confirms that Windows Installer returns valid primary icons through `ProductIcon` even when that registry value is absent. The corrected test requires native icon decoding and exact approved icon bytes. No development package was installed on the user's machine for this investigation.

The user then expanded the leaf-art scope to four botanical SVG families with all existing colors and explicitly retained every platform's application-icon requirements. Four SVG masters and sixteen recolors are inspected; the renderer's actual pile, fade and charging frames are reviewed. All species and mirrors pass continuous-rotation damage-bound tests. A local maximum-pile rendering probe measured about 7.5 ms per frame with a fixed 144 KiB sprite cache; this is local timing evidence, not physical Pi qualification. The corrected artwork and icon changes require a new exact-source qualification cycle.

The first SVG-enabled Linux builds at `d5484a5a56354a1c2137240d56e3d3a181d425ed` caught one remaining login-startup call using the former desktop-entry signature. Retain its existing theme-icon behavior while passing the explicit icon argument, and exercise desktop-entry generation on every test host. Cancel the superseded candidate 34324096405 and qualify the corrected source.

All 33 standalone source checks pass at `055a85bae8503ff80388026ffcee31bacd59bdf0`. Candidate 34324415730 proves the Global MSI's actual cached artwork, corrected shortcut and all three native GUI routes; PowerShell and Global EXE entry checks also pass. It then fails while resolving the Corporate MSI's primary icon descriptor, after its real shortcut icon has decoded successfully. The fixture now expands environment-variable icon paths before native decoding and records the original descriptor before validation. A local probe verifies expanded and direct paths resolve to identical approved icon bytes while missing files remain rejected. Per-user native confirmation remains required; do not count the failed candidate as completed qualification.

Candidate 34326512443 at `c025e333589d70435c77538424305c831376a4c7` passes all five installer transitions on native Windows x64 and ARM64, including Corporate MSI. Its recorded primary-icon descriptor uses `%APPDATA%/Microsoft/Installer/.../GooseIcon.ico`; expansion resolves the actual Windows-cached ICO and its approved artwork hash. The same candidate fails at Mac disk-image creation with `hdiutil: create failed - Resource busy`, so the overall candidate is not qualified.

Inspection of the pinned Native SDK GTK host confirms that its supplied icon path is stored but not applied. Linux now retains the existing visible launcher while adding a hidden record and full-resolution theme icon for the existing settings application identifier. Native GIO/GTK checks resolve the installed records and compare the icon GTK actually selects against approved artwork; Debian removal and shell rollback checks include the new owned integrations. These changes require fresh source and package qualification.

At `97856fa55688794997384040cfa5c566b2c85b09`, CI 34328703762 passes the installed native GTK lookup: the hidden settings identity resolves through GIO, GTK 4.14.5 selects the 512-pixel application artwork with SHA-256 `83a885f462599909f1f798541801bc3c6659836f763953ada9d250b60e4a99a5`, and the existing launcher remains the only visible entry. The genuine Debian fixture updates and restarts successfully.

Standalone GNOME 34328703661 then fails the Debian ARM64 external-revocation status check. Observations, pointer state, drag target and riding are safely withdrawn, but the provider returns an unavailable initial-consent read instead of revoked. [ADR 0053](../adr/0053-atomic-gnome-consent-reads.md) records the atomic rename/open race reproduced against production JavaScript and the bounded correction. All sixteen production-code cases pass after the fix, including repeated replacement, malformed content, unsafe ownership/modes, original deadline and cleanup. The native test oracle remains unchanged; the corrected source requires fresh complete qualification.

- [x] Required local and Native SDK checks, real renderer inspection and staged Windows routes.
- [x] Exact-source standalone native/platform checks.
- [x] Complete candidate, including native Windows packages, fresh signed/notarized/stapled universal Mac app and DMG, and both Debian packages.
- [x] Unchanged-source main qualification.
- [x] Immutable complete publication and all eight fresh-public native lanes.
- [x] Independent public asset verification and matching website guidance.
- [ ] Official updated Windows installation, application entries and actual tray/GUI behavior.

All three gates pass at the unchanged release source `630f754a0ef657172b0a6a7edea65593fa514de5`: 33 standalone checks, all 45 candidate jobs, and 37 main checks. [Candidate 34330519291](https://github.com/RealEmmettS/goose/actions/runs/34330519291) includes native Windows x64/ARM64 installation checks, signed/notarized/stapled Mac app and DMG, and both installed Debian packages. Retained Windows results cover all five installer origins on both architectures and thirty real native settings launches with icons, loaded controls and authoritative owned receipts despite stale external records. Both Debian architectures resolve the approved application artwork through native GTK.

The first Linux x64 job in [main CI 34333973952](https://github.com/RealEmmettS/goose/actions/runs/34333973952) passed installed GTK identity lookup and the tray/CLI/TUI updates, then its settings process exited with `Gtk-WARNING: Failed to open display` before a window opened. Preserve that failed attempt's logs. A single unchanged-source job rerun passes the complete GUI update, no-op and failed-update state-preservation checks, X11/Wayland smoke and target checks. No production change or test relaxation was made for this display failure; its precise host cause is not established.

The immutable v1.11.0 tag names that qualified source. [Public release build 34337228820](https://github.com/RealEmmettS/goose/actions/runs/34337228820) passes all 45 jobs and publishes [Goose v1.11.0](https://github.com/RealEmmettS/goose/releases/tag/v1.11.0) at 2026-09-09T10:25:43Z. [Fresh-public run 34340247381](https://github.com/RealEmmettS/goose/actions/runs/34340247381) passes all eight native lanes from that same source, covering Windows managed installations, Mac managed apps, Linux shell installs and both Debian packages.

Independent public-byte verification downloads all 47 assets totaling 294,722,396 bytes, including 22 payloads and their metadata. Every GitHub asset size/digest, SHA-256 sidecar, aggregate checksum and rebuilt manifest agrees. All five versionless update-control URLs match the exact-tag copies. Evidence is `target/v1.11.0-public/verification.json`; the complete gate snapshot is `target/desktop-polish-review/gate-status.json`.

The website publishes the matching graphical-control guidance through [PR 14](https://github.com/RealEmmettS/desktop-goose-site/pull/14), merge `9c305df95ba1ba82ce4e14f4c95abf02f8709964`. Production deployment 6347719130 succeeds at 2026-09-09T10:32:35Z. Final PR and post-merge CI pass, including browser checks on Windows, Mac and Linux. Post-deployment verification confirms 12 served code/font/artwork files, all 44 latest/exact-tag URL mappings, and all 22 downloaded payload identities. Live Chrome review covers the three platform guides, preferred commands, release details, leaf/petting copy and preserved r6 artwork. Evidence is `output/design/v1110-release-handoff.json` and its three verification files in the website repository.

The installed Global MSI updater discovers v1.11.0 correctly. Immediately before invoking its official update, the open old settings window has no unsaved edits and the runtime is stopped. The configuration hash is `079af46215009bfa288894781b2c97ed96581baf9beefc430689bc342b0da5c6`; login startup and petting feedback remain enabled. The clean old window closes. Windows then reports `The operation was canceled by the user` at its administrator prompt, and the updater returns failed with unchanged activation. Readback confirms v1.10.1 Global MSI at `127cd148e0af0cf76ecd6a7954777c23c156a862`, matching companion hashes, unchanged configuration and a stopped runtime. Evidence is `target/desktop-polish-review/cancelled-update-preservation.json` with the original update output and baseline. Final installed-window/icon/Search/tray checks remain open until administrator approval permits the update.

Historical releases and assets stay immutable. Physical Mac permissions, screen-reader use and experimental Pi acceptance remain separate from hosted automation.
