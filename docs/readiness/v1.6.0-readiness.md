# Honk300 v1.6.0 release readiness

Opened 2026-09-08. Refinement stage three, tasks #rkde, #wlk and #wlp. ADR 0044
owns the distinct KWin and portal authority boundaries; ADR 0045 records the shared
editable vector export. Public stable remains v1.5.0 until this stage is qualified.

The candidate adds explicit KDE setup/removal, native window observations and bounded
owned-prop positioning, plus separately requested ephemeral KDE 6 portal pointer access.
CLI and Native SDK settings reach the existing Rust runtime owner. Default native
Wayland behavior and installation/update provenance keep their existing contracts.

## Required gates

- [x] Actual KDE 5/6 x64/ARM64 script and authenticated Rust transport qualify bounded actions and refusal paths.
- [x] Actual production runtime qualifies owned-note positioning, removal, crash recovery and retained notes.
- [x] Native settings qualifies explicit KDE consent, cancellation, removal, modal isolation and unsaved drafts.
- [x] KDE 6 x64/ARM64 native portal consent grants a real libei pointer device with actual bounded motion and protected-target refusal.
- [ ] Actual runtime/settings pointer lifecycle qualifies denial/retry, engine motion, cancellation, adapter/device loss and stop/crash recovery.
- [ ] Complete versioned local Rust/Python and separate Native SDK test/check/build gates.
- [ ] Final-source CI, all eight settings lanes and all four native KDE desktops pass.
- [ ] Complete same-source candidate passes every portable, native installer, Debian and signed/notarized/stapled Mac gate.
- [ ] Merge unchanged qualified source and pass required main checks before creating a new immutable tag.
- [ ] Publish atomically, then verify every fresh-public architecture/install lane and exact artifact identity.
- [ ] Update and verify website guidance against the public release.

## Native evidence

[Native run 34203908233](https://github.com/RealEmmettS/goose/actions/runs/34203908233)
passes all four native KDE desktops at source `1830aaac29eb6c5ff94f3331c900f6de94b41387`.
The runtime, native settings and portal premises have separate results, with actual
window identities, movement, complete consent trees and revocation observations.

The subsequent runtime wiring is under integrated desktop qualification in
[run 34205879636](https://github.com/RealEmmettS/goose/actions/runs/34205879636).
Both KDE 5 lanes pass. On both KDE 6 architectures, native settings exposed the
offscreen pointer button but the SDK refused its activation before keyboard dispatch.
The real SDK test harness reproduced `InvalidCommand`; the scoped, hash-pinned patch
scrolls the existing ancestor containers before rechecking the focus target. The same
test now passes and verifies disabled controls remain inert. Actual native repetition
remains required before this integrated gate can close.
Local strict Clippy, complete workspace tests and Native SDK tests/check/build pass on
its predecessor package version; versioned final-source gates remain required.
The actual engine regression reproduced a queued warp surviving capability loss and
passed after clearing queued cursor commands when the capability is revoked.

The reusable native KDE workflow is a required dependency of candidate and public
publication. It checks out the release's exact commit and builds production Rust and
Native SDK binaries. All existing eight architecture/package lanes remain required.

## Earlier failures and limits

Earlier native probes exposed a pre-map GDK application-id setter, incomplete private
desktop service registration and missing packaged compositor plugins. Production code
now applies the prop identity after mapping; fixtures use the distribution's normal
Plasma services and PipeWire ordering. No compositor permission bypass was enabled.
Intermittent earlier ARM note movement failures remain in their evidence; final-source
native qualification must pass the unchanged motion and expiry oracles.

Hosted software-composited desktops are distinct from physical KDE, Mac and Pi acceptance.
Historical Windows elevation acceptance remains open. KDE 5 supplies no stacking-order
authority for pointer control, musl does not dynamically load these pointer libraries,
and no DND or other compositor capability is inferred from KDE results.
