# Honk300 v1.10.0 release readiness

## Publication blocked; tag retained

The immutable tag names `0ff0ffa8f70787da13dec8282c1c12d273ac784f`.
All 33 source, 45 candidate and 37 unchanged-main gates passed. Final publication
[34300623596](https://github.com/RealEmmettS/goose/actions/runs/34300623596)
failed in older x64 GNOME job
[102306638180](https://github.com/RealEmmettS/goose/actions/runs/34300623596/job/102306638180)
at the external durable-consent revocation assertion. The runtime log records
`GNOME observation failed during consent before query` and immediately clears
observation and ride authority. It does not report a completed revocation.

The test used `Path.write_text` on the active consent record, exposing a truncated
or incomplete JSON file. Production removal already uses a flushed private file
and atomic rename. Production Shell cases reproduce the classification: empty
and partial records are unavailable; a complete revoking record is revoked. All
three withhold observations and join their bounded resources. The fixture
correction retains the exact native revocation and worker-retirement assertions.

The incomplete publication was cancelled. No release assets were published;
v1.9.0 was the last verified public release at cancellation. The corrected
stage is now published and verified as v1.10.1. Do not move or delete this tag.
Continue the complete manners stage and full qualification as
[v1.10.1](v1.10.1-readiness.md). Earlier qualification history follows.

Opened 2026-09-08 for audit R05, task #prs and ADR 0049. This release follows
the independently qualified compositor releases. It adds Mac fullscreen manners
through existing Accessibility permission and reports fullscreen and DND
observations separately in the CLI, TUI and Native SDK settings.

Mac DND remains unsupported: the private Focus authorization experiment has not
qualified an app-specific grant and actual on/off signal. Linux fullscreen
comes from the explicitly enabled desktop adapter; its DND capability remains
unsupported. Windows retains its notification-availability mapping and can
recover its capability after a successful poll following failure.

## Final-source Mac accessibility correction

Source b993b89 passes all 33 standalone gates, including both real Mac presence
suites, all compositor matrices and three actual Start/Stop cycles per native
settings lifecycle lane. Final acceptance reconciliation then demonstrates two
Mac settings provider defects under ADR 0051: background actions remain available
behind a dialog, and native text edits can change only the reader snapshot.
Candidate 34287505007 was cancelled before publication; no tag was created.

Independent [34292293311](https://github.com/RealEmmettS/goose/actions/runs/34292293311)
passes both corrected production Mac readers, including native saved text, modal
isolation, cached-action refusal and separate status labels. Integrate GNOME
source f099a2e's identical shared correction. The final native settings matrix
also explicitly transfers focus away from the editor and back, and requires the
independent presence labels for this release. Runtime, observer, engine and
reviewed art are unchanged from b993b89 by this correction. Repeat all 33 source,
complete candidate, unchanged-main and fresh-public gates, preserving verified
GNOME publication and website completion before this release's tag.

## Native evidence

The final integration includes GNOME source 22810d6's shared startup correction
before either pending stage is tagged. Production Rust, Native SDK and native
smoke code remain identical to locally qualified 6badb02; the integrated source
repeats all native, candidate, main and ordered public distribution gates.

All 33 standalone source, settings and native gates pass at 2285eb0, including
both Mac fullscreen/denied-identity suites and all four compositor matrices.
Candidate [34284306818](https://github.com/RealEmmettS/goose/actions/runs/34284306818)
then fails the Intel native settings Start control with `Broken pipe (os error 32)`;
cancel the incomplete candidate. The production readiness loop treated a dropped
connection as immediately fatal. R14 records the bounded read-only retry correction,
reproducing recovery/deadline regressions, and unchanged permanent-failure behavior.
The failed capture and snapshot remain evidence. Repeat the complete final-source
native, candidate, main, public and website gates after correction.

The final integrated source includes f25e700's stopping/reversal correction under
ADR 0050. Native diagnostic 34280092759 establishes the previous long-leg failure
in actual engine foot state. Keep the corrected engine and its four World
witnesses identical to the GNOME stage, including the inspected turn fixtures.
The Mac observer implementation is unchanged. Repeat all integrated local/native,
candidate, main, immutable publication and fresh-public gates; GNOME publication
and its verified website update must complete before this stage's tag.

[Run 34249705542](https://github.com/RealEmmettS/goose/actions/runs/34249705542),
source `11f6c3479aa842abb228352c37db290ca10d80b8`, passes the complete native
Intel and Apple Silicon Rust build, strict lint, workspace tests and actual
fullscreen runtime scenarios. Both fullscreen screenshots were inspected. Freezing only the
private foreground fixture withdraws stale fullscreen state while a real status
request returns in 0.295 seconds; graceful stop takes 1.715 seconds. Actual
fullscreen entry/exit, live saved preference changes, missing-window fallback
and recovery all pass. Intel status returns in 0.157 seconds and graceful stop
takes 1.473 seconds.

[Run 34253150265](https://github.com/RealEmmettS/goose/actions/runs/34253150265),
source `48ff0785722ad6225d8f5b233bf783c6609c9740`, qualifies both architectures
after correcting expired observations to report failed rather than unprobed.
Both native fullscreen captures were inspected. Apple Silicon status during a
frozen target returns in 0.330 seconds and graceful stop takes 1.234 seconds;
Intel takes 0.132 and 1.319 seconds respectively. Both also pass the separate
private uninstalled LaunchServices app: Accessibility and fullscreen are denied,
DND stays unsupported, reload works and graceful shutdown completes without any
permission grant or installation. Final complete candidate/main/publication
gates repeat the native checks after integration of the GNOME review fixes.

[Run 34255297607](https://github.com/RealEmmettS/goose/actions/runs/34255297607),
source `a9d8a896b1dae1042b7d78489adbf7312da35c19`, repeats both Mac architectures
and private denied-identity scenarios after integration of the four GNOME review
corrections. Both actual fullscreen captures were inspected. Intel frozen-target
status returns in 0.232 seconds and graceful stop in 1.725 seconds; Apple Silicon
takes 0.346 and 1.272 seconds. Denied-app shutdown takes 0.119 and 0.226 seconds,
without a permission grant or installation. Subsequent GNOME-only timeout/input
corrections preserve this Mac implementation; complete final-source gates still repeat.

The Windows PC passes isolated native UI Automation through the real settings
executable: named controls, switches, numeric edits, modal isolation, complete
dirty-page saves and independent fullscreen/DND labels. The runtime remains
stopped, the normal user configuration is untouched, and the settings process
closes cleanly. Evidence is `target/presence-local-uia/result.json` and its
native platform-status tree. This is separate from official installed acceptance.

The additive bounded presence frame preserves the legacy status layout. Actual
worker tests exercise an in-flight query across target generation change and
permission withdrawal, plus expiry of a genuinely delayed query result. Native
desktop scenarios remain required before claiming platform qualification.

## Required publication gates

Incomplete candidate 34261907400 is cancelled after the GNOME candidate and
integrated PR capture a native input failure before the final Mutter grab.
The corrected fixture uses GTK's standard client-area WindowHandle and waits
for real pointer entry. All actual grab, goose ride, terminal exclusion and
held-action interruption checks remain required; production presence code and
permission bounds are unchanged. Final-source native gates repeat before a
replacement complete candidate.

Both native Mac lanes pass again in 34264286000. GNOME run 34264286015
passes three desktops; the newer x64 capture receives only preparatory motion,
with no final GTK motion or button gesture. The final-stage fixture now waits
for each separate native motion and the actual primary-button press before
issuing the next input step. Passive controllers preserve ordinary event
propagation. The real Mutter grabs, engine rides, protected-terminal refusal
and held-action interruption checks are unchanged. Preserve that failed run
and repeat the final-source desktop and complete candidate gates.

Run 34267024884 delivers the separate native motions on all four desktops but
the Python bindings pass a null raw GdkEvent to the diagnostic callback. GTK's
typed pressed signal now records the actual primary button while leaving its
sequence unclaimed, so ordinary WindowHandle propagation and native movement
continue. The strict button acknowledgment is retained. Preserve all four
binding failures and repeat native qualification on this corrected fixture.

Both ARM64 suites pass 34268128789. The x64 captures fail before any held
gesture, with GTK receiving no preparatory or final motion and reporting no
pointer over the client. The private input point now stays left of the goose's
centered return route; its first move must differ from the current pointer.
Mutter's actual picked surface is recorded before, after and on failed input.
All native motion/button acknowledgments, real grabs and engine checks remain
required. No production input capability or behavior is changed.

All four e1cfaa7 desktops receive the corrected native drag input. Three
complete suites pass; older ARM64 cancels its real held ride and withdraws
observations but reports unchanged consent as revoked after extension disable.
The production-method regression in c2827ff reproduces that timing-dependent
status and corrects it to Unavailable/failed, preserving actual durable
revocation and every cancellation assertion. Integrate this correction and
repeat complete final-source qualification. The Mac observer is unchanged.

Source `420dc31` passes all four GNOME desktop suites in 34271715326 and both
native Mac presence suites in 34271715256. Newer-ARM delivered-note capture
inspection exposes a shared engine stopping-art defect despite the functional
passes. Cancel candidate 34271817809 and integrate 2b050ac's actual FeetState and
World reproductions and the correction under ADR 0050. The foot withdraws its old
walking prediction while retaining planted contacts and original step/lift phase.
All native visual and complete final-source gates repeat; the Mac observer is
unchanged, and previously completed jobs do not qualify this changed source.

- [x] Qualify real fullscreen, live settings, target interruption and shutdown on Intel and Apple Silicon.
- [x] Qualify denied presence through a private uninstalled Mac app without granting permission.
- [ ] Verify separate native accessibility labels and actual Linux adapter status across their supported desktops.
- [x] Pass versioned local formatting, strict lint, Rust/Python tests and release build.
- [x] Pass separate Native SDK tests, strict check and actual build.
- [ ] Pass final-source PR gates and all native architecture/settings lanes.
- [ ] Pass the complete candidate with all eight archives, native installers, Debian packages and signed/notarized/stapled Mac artifacts.
- [ ] Merge unchanged source and pass main gates.
- [ ] Publish a new immutable release after GNOME public/website verification and verify every fresh-public architecture/install lane and artifact identity.
- [ ] Update and verify website guidance against the public release.

The two native Mac presence jobs are mandatory candidate and publication
dependencies, alongside all separate compositor and existing package gates.
Physical Mac permission acceptance, official Windows elevation acceptance and
physical Pi performance remain open. No old release asset is changed.
