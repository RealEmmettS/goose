# honk300 v1.3.0 Release Readiness

Date opened: 2026-07-19

Target: stable/latest `v1.3.0`

Release task: `#wstart`

Architecture decision: ADR 0036

## Intent

v1.3.0 makes a Windows terminal a bounded start controller instead of the owner of the running
goose. Every public start spelling resolves the branded exact-sibling `honk300-app.exe`, forwards
the complete start option vector, waits for local IPC readiness, and returns. The transient GUI
launcher starts the hidden console-free `honk300.exe` runtime, which retains the authoritative
singleton, overlay, IPC server, and notification-area item after the originating terminal closes.

The release preserves the existing CLI grammar, TUI, config schema, IPC protocol, tray actions,
installer receipt schema, and update provenance. It also gives the user-pinnable app launcher a
binary-specific multi-resolution icon derived from the canonical goose status artwork.

## Source and local gate

- [x] Record the changed Windows process boundary in ADR 0036 and synchronize current guidance,
  technical and human changelogs, and the SHAUGHV task board.
- [x] Add hidden-command parsing/help exclusion, option-forwarding, exact-sibling/missing-launcher,
  readiness/failure, singleton-race, TUI, packaging, and lifecycle regressions.
- [x] Pass formatting, strict workspace Clippy, locked workspace tests, x64 release build, Python
  contracts, PowerShell parsing, actionlint, RustSec, cargo-dist planning, and package-shape proof.
- [x] Prove on physical x64 Windows that a short-lived PowerShell and transient app exit while the
  exact hidden runtime, IPC, and tray owner survive; duplicate start preserves that runtime and
  graceful stop finishes.
- [ ] Re-run the clean committed-tree package-list gate for exact v1.3.0 candidate bytes.

## Candidate, main, publication, and public bytes

- [ ] Pass the exact v1.3.0 candidate across all platform producers and final assembly, including
  disposable Windows x64 and native ARM64 PE/icon/installer/lifecycle qualification.
- [ ] Fast-forward the unchanged candidate commit to main and pass ordinary same-SHA CI.
- [ ] Create immutable v1.3.0, publish the complete asset set atomically, and verify manifest
  identity against the exact tag and commit.
- [ ] Pass all eight fresh-public-byte jobs for Windows x64/ARM64, Mac Intel/Apple Silicon, Linux
  shell x64/ARM64, and Debian amd64/arm64.
- [ ] Verify exact-tag/latest byte identity and the live command-first production website.

## Closure

- [ ] Record exact run IDs, commit, asset count, public hashes, latest aliases, and site evidence.
- [ ] Complete `#wstart`, retain clean synchronized main, and remove the merged temporary release
  branch locally and remotely.

## Evidence log

- 2026-07-19 — complete working-tree qualification passed: strict native workspace Clippy, 443
  locked workspace tests including the concurrent-launcher regression, locked x64 optimized
  build, 122 Python contracts with three honest platform skips, PowerShell parser, actionlint,
  cargo-dist planning, RustSec over 418 locked dependencies, 108-file package shape, x64 console-3
  and GUI-2 PE identity, distinct extracted launcher icon, and ARM64 resource cross-check.
- 2026-07-19 — bounded physical x64 proof started from a short-lived PowerShell. The controller and
  transient app parent exited; the hidden runtime retained IPC and the exact tray-owner window;
  duplicate start preserved the same runtime; graceful stop completed; no Honk300 process remained.
