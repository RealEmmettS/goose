# Honk300 v1.9.0 release readiness

Opened 2026-09-08 for the separately qualified GNOME follow-on, task #rgn and
ADR 0048. Publication follows Hyprland. The candidate adds an explicitly installed
Shell companion for window, fullscreen and actual user-drag observations, backed
by Rust ownership and Native SDK consent controls. It keeps the compatible
XWayland overlay and independent owned-prop positioning. Foreign-window movement,
pointer access and DND remain unavailable.

## Completed publication

Published 2026-09-09T01:29:03Z from exact immutable source
`f099a2edb7639e866d94286d8199a29d7ab92146`. This release completes its local, native, candidate,
unchanged-main, public-download and website gates. Earlier pending statements
below preserve qualification history; this section records the final outcome.

- Final source: 31 passed jobs and 3 expected skips across [CI 34293019835](https://github.com/RealEmmettS/goose/actions/runs/34293019835), [Native settings 34293019594](https://github.com/RealEmmettS/goose/actions/runs/34293019594), [Native GNOME adapter 34293019620](https://github.com/RealEmmettS/goose/actions/runs/34293019620), [Native KWin companion 34293019675](https://github.com/RealEmmettS/goose/actions/runs/34293019675), [Native Sway adapter 34293019609](https://github.com/RealEmmettS/goose/actions/runs/34293019609), [Native Hyprland adapter 34293019585](https://github.com/RealEmmettS/goose/actions/runs/34293019585).
- Complete candidate: 43 passed jobs in [Release 34293903609](https://github.com/RealEmmettS/goose/actions/runs/34293903609).
- Unchanged main: 35 passed jobs and 1 expected skip across [CI 34295584697](https://github.com/RealEmmettS/goose/actions/runs/34295584697), [Native settings 34295584905](https://github.com/RealEmmettS/goose/actions/runs/34295584905), [Native GNOME adapter 34295584447](https://github.com/RealEmmettS/goose/actions/runs/34295584447), [Native KWin companion 34295584463](https://github.com/RealEmmettS/goose/actions/runs/34295584463), [Native Sway adapter 34295584467](https://github.com/RealEmmettS/goose/actions/runs/34295584467), [Native Hyprland adapter 34295584466](https://github.com/RealEmmettS/goose/actions/runs/34295584466).
- Immutable publication: 43 passed jobs in [Release 34297106655](https://github.com/RealEmmettS/goose/actions/runs/34297106655).
- Fresh public distribution: all eight native architecture lanes pass in [Post-release Installer Smoke 34299610331](https://github.com/RealEmmettS/goose/actions/runs/34299610331).

Publication includes all eight platform archives, both Windows installer families
and architectures, both Debian packages, and a freshly signed, notarized and
stapled universal Mac app and DMG. The separate settings companion retains its
exact release identity and installation ownership.

Independent public verification downloads 291,461,316 bytes across
47 assets and 22 payloads. Exact-tag manifest identities, platform/architecture,
sizes, GitHub digests, SHA-256 sidecars, aggregate checksums and the five stable
latest controls agreed at verification time. The local record is `target/v1.9.0-public/verification.json`.

### Final native and visual evidence

Both final production Mac settings providers pass 11 independent native
Accessibility checks on Intel and Apple Silicon with SDK automation disabled.
The reader verifies actual names, actions, switch values, explicit focus transfer,
native text save, modal isolation and refusal of cached background actions.

Each native settings lifecycle lane completes three actual GUI Start/Stop cycles.
All four final-source GNOME suites pass; all eight delivered-goose captures and
four native settings/goose captures were inspected. Earlier 48-delivery motion
qualification uses the same production engine and remains preserved below.
Local formatting, strict workspace lint, all Rust tests, the release build,
138 Python checks (three platform skips), and separate Native SDK tests, checks
and real production build pass. Prior modal, text-persistence, startup and gait
failures remain documented with their corrections; no failed oracle was relaxed.

### Website and acceptance boundaries

Website [PR 12](https://github.com/RealEmmettS/desktop-goose-site/pull/12)
deploys commit `f0129695a75d69d17353a707bf8b5ac9e7037f6d` through successful deployment
6340711375. All four post-merge jobs in
[CI 34300104238](https://github.com/RealEmmettS/desktop-goose-site/actions/runs/34300104238)
pass. The website handoff verifies actual served assets, browser behavior,
platform guidance and all 22 payload mappings against the public release audit.
The user's selected r6 artwork is retained; application renderer exports remain
available separately.

The user's Windows v1.3.5 installation and protected receipt remain unchanged:
read-only SHA-256 verification at 2026-09-09T03:10:59.7771199Z matches both
historical fingerprints. The local record is
`target/refinement-installed-preservation.json`.
Official installed Windows acceptance, physical Mac permissions and screen-reader
use, and physical Pi performance/acceptance remain open. Pi 4/5 64-bit Desktop
support remains experimental; Mac/Linux DND remains explicitly unsupported.
Hosted native and public-distribution evidence does not close those physical gates.

## Qualification history: Mac accessibility correction

Source 22810d6 passed all 31 standalone gates, including three real settings
Start/Stop cycles per native lifecycle lane and four independently reviewed GNOME
desktops. Final acceptance reconciliation then found a real Mac provider failure:
enabled background actions remained behind a modal, and native text edits could
change the reader snapshot without reaching the saved draft. Candidate
34287468666 was cancelled before publication; no tag or public asset was created.

ADR 0051 and audit R15 retain the original native failure trees. Independent run
[34292293311](https://github.com/RealEmmettS/goose/actions/runs/34292293311)
passes both corrected production readers on Intel and Apple Silicon, including
native text save, modal isolation and refusal of cached background actions. The
shared exact-hash SDK patch is now integrated here. Production Mac settings
qualification also explicitly moves focus away from the editor and back. The
release, main and public gates must repeat from this final source; prior SDK
harness passes do not replace actual native Accessibility evidence. Rust, engine
motion and reviewed art are unchanged from 22810d6 by this correction.

## Native evidence

Source f25e700 passes all 31 standalone gates and all 43 jobs in complete candidate
[34283702176](https://github.com/RealEmmettS/goose/actions/runs/34283702176).
The four-desktop diagnostic [34282484203](https://github.com/RealEmmettS/goose/actions/runs/34282484203)
passes 48 actual deliveries using identical production engine/runtime code. Every
delivered-goose capture and the worst settling sequences were reviewed. Maximum
observed stopped vertical foot offset is 7.13 pixels, versus 43.60 in the retained
failing diagnostic; existing gait and golden comparison limits are unchanged.
The source's four GNOME suites, all native settings and owned-image captures pass
and were inspected. The first Hyprland ARM64 setup query timed out during private
shader compilation before the goose started; the unchanged-source repeat and all
candidate Hyprland lanes pass, with the original logs retained.

Before creating a tag, final-stage candidate 34284306818 exposes a broken startup
connection rejected immediately by the shared readiness helper. Carry R14's bounded
read-only retry correction into both pending stages and repeat final-source gates.
The native GUI now requires three actual Start/Stop cycles per lifecycle lane and
retains status before failure cleanup. No engine/art change accompanies this fix.

Final delivered-note review held publication after the original stopping fix.
Diagnostic run 34280092759 captures the same stale airborne prediction and delayed
planted foot on both Debian GNOME architectures. ADR 0050 records the actual
World reproduction, current-motion landing correction, preserved planted contacts,
reviewed two turn-golden updates and native-height motion exports. Final local
workspace, release, Python, production JavaScript and separate Native SDK checks
pass. All same-source native/candidate/main and public gates repeat on this source;
earlier passing suites and cancelled candidates do not qualify the correction.

[Run 34226098889](https://github.com/RealEmmettS/goose/actions/runs/34226098889)
qualifies the actual GNOME Shell 46.0 and 48.7 interface on native x64 and ARM64:
extension discovery and enable/disable, exact window identity, fullscreen,
Mutter's actual user-grab signals and a complete visible goose outside overview.
The private fixture's window movement/focus methods do not ship in the companion.

[Run 34237848251](https://github.com/RealEmmettS/goose/actions/runs/34237848251)
passes the initial complete production runtime and native-settings lifecycle on
both x64 Shell versions, including actual ordinary/terminal drags, fullscreen
manners, independent settings drafts, live revocation and graceful/crash recovery.
This does not complete the later held-ride interruption and owned-prop gates.

[Run 34243427852](https://github.com/RealEmmettS/goose/actions/runs/34243427852)
passes the complete production runtime/settings and both owned-prop suites on
GNOME 46.0 and 48.7, each on native x64 and ARM64. The provider now omits destroyed
and empty actors during creation/remapping while Rust still rejects invalid
reported geometry. Actual held-input cancellation, extension loss, explicit
reconnection, saved drafts, independent process lifetimes, rendered note text,
uncropped images, capacity, native close, malformed input, EOF, failed helpers
and graceful/crash recovery all pass.

All four native settings/goose captures, all four delivered-note captures and all
four complete-image/capacity captures were inspected. The final settings layout
adds space inside the scrolling content so its scrollbar cannot cover the end
of a wrapped line; the final-source native gates repeat that visual check.

Versioned Windows-host formatting, strict workspace clippy, Rust tests, release
build and 137 Python tests passed. The separate versioned Native SDK tests, check
and production build passed; final-source architecture builds remain required.

Run 34252264898 repeats all four desktop suites and twelve inspected captures
after final scrollbar/input-fixture corrections. Subsequent PR review confirms
four additional defects: omitted embedded source files, blind XWayland pointer
input, transient actors counted before filtering, and periodic consent reads on
the runtime loop. Their correction includes real Cargo selection, production
JavaScript window-bound/cancellation checks and the retained worker's revocation
regression. Full native and publication gates must repeat on the corrected source;
the earlier desktop passes do not qualify those later changes.

Diagnostic run 34256746560 identifies a real 200 ms timeout during Shell's final
consent check. Late/busy queries now withdraw all old data and may retry only on
the retained authenticated connection, with full owner/consent checks every time.
No timeout is extended and revoked, replaced, malformed or disconnected sources
still end the worker. Production request cancellation/recovery and the retained
worker's stale-frame/revocation regression pass; full native qualification repeats.

Run 34258094137 passes the complete newer-ARM lifecycle. The older ARM runtime
correctly withdraws its ride but the 64-transition presence log ends before the
last assertion; the fixture now reads actual manners from its existing bounded
post-tick engine trace. The x64 captures identify late input failures around
remapped fixture decorations. Explicit GTK title bars and distinct real client
motion preserve the mandatory native grab, terminal and held-ride assertions.
Consent checks now read independent files concurrently with all original bounds,
identity checks and joined cancellation cleanup; production JavaScript tests
exercise those actual reads and refusal paths. All native gates repeat again.

[Run 34260153471](https://github.com/RealEmmettS/goose/actions/runs/34260153471)
passes the complete corrected GNOME suites at
`99adb7efa31ca03b719ab5299695eb9022c3c59c`: both Shell versions on x64 and ARM64,
all four runtime/settings lifecycles and all eight prop cycles. All sixteen result
records pass. Native settings/goose, painted-note and complete-image captures were
inspected for every lane. One newer-x64 settings capture includes a private test
window over part of the page; the earlier matching settings layout and the other
three final captures retain the unobstructed layout evidence. Every sampled pointer
remains absent as required. Each complete runtime logs only twelve presence
transitions after concurrent consent reads, including the actual requested
fullscreen/removal cases, instead of exhausting the earlier transition log.

The complete candidate 34261317929 then exposes a late newer-ARM gesture that
never reaches Mutter: GTK records pointer leave before the frame button press,
and the runtime continues fresh observations. Integrated run 34261718352
reproduces that input failure. Private fixtures now use GTK's actual WindowHandle
inside the client and await its native pointer entry before the real gesture.
No synthetic grab, extended deadline, waived ride or cancellation assertion is
introduced. Both incomplete candidates are cancelled; qualification repeats on
the corrected fixture source.

Run 34262984345 records native motion delivered inside the new drag handle while
the dynamically attached controller's contains_pointer property remains false:
the pointer was already over that widget, so no enter event was replayed.
Qualification now checks the actual newly captured motion/enter coordinates
against current handle bounds and native surface ownership before pressing.
Mutter's real grab and all subsequent engine assertions remain mandatory.

## Required publication gates

Source `c2827ff` passes all 31 standalone gates, including all four complete GNOME
desktops in run 34271472853. Integrated native run 34271715326 then exposes a
shared engine art defect in a delivered-note capture: an airborne foot can keep
reaching toward its old walking target after the goose stops. Cancel both pending
candidates 34271545636 and 34271817809. ADR 0050 and task #rst record the actual
FeetState/World reproductions and the correction, preserving planted contacts
and original step/lift phase. Native visual review and all final-source gates
repeat; the earlier native passes do not qualify the changed gait.

The correction passes Windows-host formatting, strict workspace clippy, complete
Rust tests (including 209 engine tests and seven unchanged visual goldens),
release build and 138 Python tests with three existing platform skips. All eight
production Shell JavaScript regressions and separate Native SDK test, check and
real production build pass. Sixteen sequential frames around each front, rear,
fast and gradual stop were inspected from the actual Rust renderer export.

Candidate 34265477487 passes all 43 jobs at
`6ff761159cf4f3bec62ec5f0a425a919178c321a`; all 31 required PR checks pass and
that exact source reaches main. Main run 34269778219 passes three desktops but
captures a pre-drag GTK input failure on newer x64. Integrated run 34269817286
also demonstrates an extension-disable/query race: the actual goose withdraws
observations and cancels its ride, but a successful native I/O completion after
disable can misclassify unchanged consent as revoked. A production regression
reproduces Revoked where Unavailable is required. The correction preserves
actual consent revocation, cancellation and stale-frame rejection, while
reporting extension loss consistently as failed. Private drag input now awaits
each GTK motion and typed button press away from the goose's return route and
records Mutter's actual picked target. No tag has been created; the corrected
source must repeat native, complete candidate and main qualification.

- [x] Independently qualify the actual Shell interface and compatible visible overlay.
- [x] Pass the complete Rust/native-settings lifecycle and held-action cancellation on all four GNOME desktops.
- [x] Inspect native goose/settings output and qualify complete note/image delivery and failure cleanup.
- [x] Pass versioned local formatting, strict lint, Rust/Python tests and release build.
- [x] Pass separate Native SDK test, check and real build.
- [x] Pass final-source PR gates and all native architecture/settings lanes.
- [x] Pass the complete candidate with all eight archives, native Windows installers, Debian packages and signed/notarized/stapled Mac artifacts.
- [x] Merge unchanged source, pass main gates and publish a new immutable release after Hyprland.
- [x] Verify fresh public artifacts on every architecture and independently audit every manifest identity.
- [x] Update and verify website guidance against the public bytes while preserving the selected artwork.

GNOME is a required same-source candidate/publication dependency alongside the
separate KDE, Sway, Hyprland and settings/platform/package gates. Private native
desktops do not establish physical Pi performance, Mac permission acceptance or
Windows elevation acceptance. Other compositor versions inherit no qualification.
