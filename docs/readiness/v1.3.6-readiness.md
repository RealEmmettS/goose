# honk300 v1.3.6 Release Readiness

Date opened: 2026-07-23

Target: stable/latest `v1.3.6`

Release task: `#r136`

Decision: ADR 0039 calmer default mud cadence

## Intent

v1.3.6 makes mud an occasional accent. Automatic puddle hops move from 70–160 to 180–300
seconds apart and track new mud for 10–30 instead of 30–90 seconds. Fresh/direct mud defaults to
10 instead of 15 seconds while existing explicit configuration remains authoritative. Puddle
travel, footprint fade, rendering, sound, manners, configuration schema/surface, platform,
packaging, updater, and lifecycle behavior remain unchanged.

## Source and local gate

- [x] Update automatic and manual defaults and record the narrow supersession in ADR 0039.
- [x] Pin exact defaults and preserve existing explicit configuration through load/save/effective
  world options.
- [x] Exercise an isolated release config without changing the operator's real config.
- [x] Synchronize Cargo metadata, canonical/current guidance, task state, README, and both
  changelogs.
- [x] Pass formatting, strict Clippy, locked workspace tests, release binaries,
  Python/PowerShell/shell/release contracts, architecture/package checks, RustSec, cargo-dist
  planning, and committed-tree verification.

## Candidate, publication, public bytes, and physical acceptance

- [x] Pass exact candidate and unchanged-main CI before creating immutable v1.3.6.
- [x] Publish the complete artifact set atomically and verify exact tag/commit/manifest identity.
- [x] Pass all eight fresh-public-byte jobs and production-site verification.
- [ ] Update/install untouched public v1.3.6 on Windows and prove provenance, receipt, runtime, and
  isolated-config behavior.
- [ ] Complete `#r136`, merge closure evidence to main, and remove merged local/remote branches.

## Evidence log

- 2026-07-23 — live `origin/main` and immutable release state were rechecked before work:
  `main` was `a7991ba96fb0bb49554293e4eb1d48ff5922e120`; v1.3.5 remained immutable at
  `9f26dc6eebd8dcc8762ba56d0e43115e37fba1d9`; no v1.3.6 branch, tag, or release existed.
- 2026-07-23 — branch `codex/v1.3.6-calmer-mud` started from that clean evidence baseline. The
  tracked board upgraded from 1.0.1 to 1.0.2 in isolated commit `41d4d90`, relaunched at its
  verified Goose identity, and task `#r136` moved directly to Active.
- 2026-07-23 — ADR 0039 and the implementation set natural puddles to 180–300 seconds apart,
  automatic mud to 10–30 seconds, and fresh/direct mud to 10 seconds. The unchanged 8–15 second
  puddle visit, 8.5-second footprint lifetime, one-second shrink time, schema v2, and stable
  config/TUI surface remain explicit invariants.
- 2026-07-23 — the focused engine/config default, puddle-state-machine, plain-wander negative,
  omitted-field, and explicit-15-second round-trip tests passed. The locked release binaries
  built as v1.3.6. Isolated `setup --reset` materialized
  `target/v1.3.6-isolated-20260723-011058/config.toml` with
  `duration_to_track_seconds = 10.0`; that config alone drove a local runtime and accepted
  `do mud` before graceful stop. The operator's real config remained unchanged with SHA-256
  `10ff8b72400412fd48373afa12aa71bac970a162bdecd2f792cf1fd8c987fda5` before and after.
  Computer Use did not expose the layered goose overlay as an inspectable application window, so
  the exact 10-second creation cutoff and normal 8.5-second residual fade are supported by direct
  engine state-machine/default tests rather than a screenshot claim.
- 2026-07-23 — complete Windows-host qualification passed: formatting and diff hygiene; strict
  native workspace plus x64/ARM64 musl Clippy; ARM64 Windows release checking; Mac ARM64 and Linux
  ARM64 platform checks; 467 locked Rust tests; 127 Python release contracts with three expected
  host skips; five PowerShell and nine shell parsers; actionlint 1.7.12; RustSec 0.22.2 over 418
  locked dependencies; eight-target cargo-dist v1.3.6 planning; exact x64 console/GUI PE identity;
  optimized 1.3.6 binaries; and the unchanged 110-file root package shape.
- 2026-07-23 — PR CI `29984758143`, exact 19-job candidate `29985258824`, unchanged-main CI
  `29985922883`, 19-job atomic publication `29986578201`, and all-eight fresh-public-byte run
  `29987240456` passed at source `1c56bf5679d04f160d09e4765a866458cad024aa`.
- 2026-07-23 — immutable v1.3.6 has 47 assets and 22 manifest payloads. Exact-tag and latest
  manifest bytes match at SHA-256
  `df56bee94836d0c6f7e5b03f85220a0f5f62e91910cda9ab49cd7de7e1e61e6d`; exact/latest
  PowerShell bootstrap bytes match at
  `ac3bd8c50eb8ac9bc23b2997e3384ae34ddaec00fa1a70da18da6860dfec4d1f`.
  Production rendered verified v1.3.6 Windows/macOS/Linux views with exact-tag review/checksum
  links, no console errors, and no horizontal overflow.
- 2026-07-23 — the official v1.3.5 self-update selected v1.3.6, then failed before installer
  launch because its generated PowerShell lifecycle-archive expression arrived as
  `Replace('','/')`. v1.3.5 and protected receipt SHA-256
  `472ca1995ba4e2d8bebbf600f575f4b4d7620314f49bd0d8c2785f3db0da5c46` remained unchanged.
  The exact public v1.3.6 bootstrap reached the genuine elevation dialog, but consent was
  canceled and no MSI ran. Installed acceptance remains open under immutable fix-forward `#r137`.
