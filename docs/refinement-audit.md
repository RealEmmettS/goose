# Honk300 refinement audit

Source baseline: `599d6fc`. Started 2026-09-07. Entries distinguish source findings from platform or physical acceptance. Final first-stage source `85c9d426f7409800198ee5e89bca7d6042034082` is published as v1.4.0 with all eight fresh-public lanes passed; see [the readiness record](readiness/v1.4.0-readiness.md). This does not imply physical acceptance on unavailable equipment.

| ID | Severity | Evidence and impact | Correction and verification | State |
|---|---|---|---|---|
| R14 | Medium | Final candidate 34284306818's native Intel settings Start shows Broken pipe instead of readiness. The production readiness loop immediately rejects a dropped connection while the child can still be starting. | Retry only startup's read-only Status probe for broken/reset connections inside the existing ten-second deadline; check child exit before every attempt and keep permission/protocol failures immediate. Production-loop recovery, deadline and permanent-error regressions reproduce the old rejection; repeat native settings and complete final-source gates. | Correction under qualification in [#prs](../.tasks/tasks/prs.md); final candidate cancelled |
| R13 | Medium | Native delivered-note captures expose a stretched leg. Actual World delivery reproduces a stale airborne prediction through stopping and a reversal that skips zero speed, delaying the opposite planted foot. | Keep airborne landing predictions current through actual motion, withdraw lead at rest and retain the existing travel cap during acceleration under ADR 0050. Preserve planted contacts and lift phase. Four World witnesses and gait checks pass; actual motion reviewed; repeat native delivery and release gates. | Corrected locally; native qualification and publication tracked by [#rst](../.tasks/tasks/rst.md) |
| R01 | Medium | Windows/Linux allocate a dirty Pixmap on every presentation; macOS alone reuses and shrinks it. Allocation churn grows with large trails and display bounds. | Shared bounded DamageCanvas owns reuse, growth, shrink and clearing on every platform. Production canvas regressions pass; measured old/new art workloads are in `docs/readiness/refinement-render-measurements.md`. | Published and fresh-public verified in v1.4.0 |
| R02 | Medium | Windows/Linux sleep 2 ms per loop; macOS's delay ignores accumulator remainder. Extra polling and drifting tick deadlines waste work. | Shared deadline includes accumulator remainder and elapsed frame work, with the existing five-tick catchup bound. Actual core timing regressions and hosted Linux/Mac runtime smoke pass. | Published and fresh-public verified in v1.4.0 |
| R03 | Medium | Linux tests assert test-only arrays, identity functions, and a buffer-count formula. Audio tests assert a test-only backend selector and child-count formula. They do not exercise the behavior named by the tests. | Removed disconnected tests; exercised real child exhaustion/reaping/recovery and actual Wayland buffer availability. Pending damage is acknowledged only after successful presentation. Workspace regressions and hosted Linux runtime smoke pass. | Published and fresh-public verified in v1.4.0 |
| R04 | Medium | Renderer switches between side and overhead drawings via opacity crossfade. Source-generated contact sheet exposes overlapping silhouettes and inconsistent proportions. | One continuous projected rig; final front/rear steps, twelve headings and task expressions inspected on light/dark backgrounds at normal/enlarged size. Geometry, planted-contact, alpha and bounds regressions pass. | Published and fresh-public verified in v1.4.0 |
| R05 | Medium | macOS presence_state returns unsupported; Linux presence_supported is false. Fullscreen/DND manners cannot be assumed from a saved toggle. | Separate fullscreen/DND status and a retained Mac Accessibility worker are implemented under ADR 0049. Run 34253150265 passes actual fullscreen, stopped-target expiry/recovery, live settings, denied identity and shutdown on Intel and Apple Silicon. Linux reports the actual enabled compositor capability and keeps DND unsupported. Complete final-source publication gates remain pending. | Open; tracked by [#prs](../.tasks/tasks/prs.md) and #wlk within the refinement milestone |
| R06 | Medium | Linux collect support is disabled, so note/meme commands cannot provide the Windows/macOS experience. | Owned companion props pass native production GNU/musl x64/ARM64 host/runtime checks (34189363866) and labwc (34186038608); positioning remains separate from compositor placement. | Published and fresh-public verified in v1.5.0 |
| R07 | Medium | Config editors load a whole model and save later without detecting intervening edits. Adding a GUI makes concurrent edits more likely. | GUI/TUI share revision-aware atomic persistence and a cooperating-writer lock. Conflict, compatible unknown-key, comment, validation and exact-runtime reload regressions pass; isolated native Windows saves read back correctly. | Published and fresh-public verified in v1.4.0 |
| R08 | Low | install.rs combines provenance, user-media migration, autostart, platform mutation, and tests in roughly 245 KB. Current guidance duplicates years of release history. | Extracted provenance, autostart, mutable-media migration and existing tests; public lifecycle API retained. Active guidance points to complete historical copies. Full Windows-host workspace regressions, strict clippy and 129 Python checks pass; native cfg builds remain part of CI. | Published and fresh-public verified in v1.4.0 |
| R09 | Medium | Existing ARM64 builds are not Pi desktop acceptance. Native SDK's Linux native-only host requires GTK4. | Exact production companions pass GNU GTK 4.6 and native Alpine checks; ARM64 labwc 0.7.1 scales one/two pass and captures were inspected. Experimental Pi guidance records installation and remaining hardware acceptance. | Published as experimental support in v1.5.0; physical Pi open |
| R10 | Medium | Rodio playback detaches sinks with no common voice bound, unlike the musl child pool. Repeated control requests can accumulate concurrent playback. | Both backends retain and cap active voices and reap completion through the same bounded admission policy. Real child-process exhaustion/recovery and the full audio regressions pass. | Published and fresh-public verified in v1.4.0 |
| R11 | Medium | Captured Windows start returns its exit code but the detached runtime keeps the caller's output pipes alive. The new native settings Start then remains Working because its service output never reaches EOF. Reproduced with exact public v1.3.7 and the real GUI fixture. | Both Rust entry points clear inheritance on their original standard handles before spawning. The same captured CLI/app test now gets EOF with the runtime alive; actual GUI Start, independent settings close and graceful Stop pass. | Published and fresh-public verified in v1.4.0 |
| R12 | Medium | Two physical Windows baseline probes delivered a note then permanently lost collect capability. The retained log reported a zero Win32 error because normal foreground denial was converted into a fatal backend error. | Owned-window focus is best effort; direct note editing and delivery do not depend on activation. The same isolated native workload completed note/meme errands, retained collect support, produced no backend errors and exited gracefully. | Published and fresh-public verified in v1.4.0 |

## Preservation contracts

No existing release tag or asset changes. Preserve saved configuration, personal media, installer provenance, protected receipts, exact-byte verification, rollback, terminal protection, and graceful shutdown. Native SDK owns only the new settings window; the Rust runtime, tray, updater, and installation remain authoritative. New platform support is advertised only at its demonstrated capability level.

## Evidence

- Baseline renderer: `target/goose-audit-current.png`, produced from the unchanged Rust preview example.
- Baseline: `cargo test --locked --workspace --quiet`, passed on Windows. Non-host OS code behind cfg gates was not executed.
- Physical macOS/Pi acceptance is unavailable by the user's 2026-09-07 hardware statement; it is not inferred from cross-compilation or hosted tests.

## Additional demonstrated interaction defect

The exact public Windows baseline repeated R12 under the startup/wander/mud/note workload;
`target/runtime-profile-v137-direct/runtime.stderr.txt` retained the failure. Microsoft's
[SetForegroundWindow contract](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setforegroundwindow)
documents a zero return for denied activation, including otherwise valid requests; it does
not specify a last-error value. The fix makes no attempt to bypass foreground policy.

Cursor seeking aimed the body at the pointer while acquisition tested beak distance. A fixed-tick world regression failed for a left/down approach after the rig redraw. Use the existing beak-offset locomotion target, as collected props already do. The real approach and permission-revocation regression now passes for three directions.

## Additional settings findings

- The TUI had no updater action. Added independent Check/Update actions through the existing update lifecycle, and a read-only CLI/service check that never enters cleanup or installation.
- Generic editor reloads could address a runtime using another configuration file. Added the bounded additive `RELOAD_IF` token for exact selected-path and saved-revision matching; ordinary CLI `RELOAD` remains compatible.
- Native SDK 0.5.4's subprocess effect used Zig's visible Windows console default. The pinned project-local SDK patch enables `create_no_window`; exact source hash checks prevent an accidental framework-wide edit or silently drifting patch.
- Native windows were inspected through the SDK's retained-scene screenshots and widget input, with a real Windows HWND confirmed for the public `settings` launcher. Screenshots establish layout and input behavior, not an OS compositor screenshot or physical-device acceptance on other platforms.
- Subsequent Windows computer use captured the actual settings HWND with Makira Bold/Light, toggled reduced motion, saved, and verified the isolated config. UI Automation exposed only a canvas pane: the pinned SDK has no Windows/GTK widget accessibility callback. Current published SDK 0.10.1 was inspected separately and still lacks the Windows callback. The initial missing native bridge was subsequently corrected and qualified under ADR 0042.
- Tray Configure originally discarded an explicitly selected runtime config path; its exact-sibling GUI launch now passes that path on all three platforms.
- Archive bundling tests preserve Rust payload bytes/modes and refresh the cargo-dist checksums. Raw ZIP member spelling is validated before Windows filename normalization, so backslashes and traversal cannot evade the same cross-platform guard.

- Native SDK accessibility is now forwarded through the bounded AccessKit bridge in ADR 0042. Actual Windows UI Automation exposes static text, named controls, edit focus and saved status; keyboard editing persisted the isolated config. The GUI and DLL both remain locked through initialization and are independently bound to the Windows receipt. GTK/AT-SPI and final native qualification passed in the v1.4.0 candidate and publication runs.
- The actual Windows and GNU Linux accessibility checks exposed a 64-widget truncation after a page gained changed-state labels, hiding Save from assistive readers. Both toolkit and bridge now retain up to 256 widgets. The isolated native Windows save and saved-status readback passed after correction; hosted OS-pattern checks passed in the final native settings matrix.
- Hosted Windows capture showed an Autumn leaf pile contaminating the goose-specific antialiased color vote. The oracle now samples only the identified goose fringe while retaining every global transparency, crop and channel check. The original failed capture now passes; injected incorrect colors, double premultiplication and cropped anatomy still fail. Paired compositor and raw-DIB prop regressions pass.
- Native Mac compilation detected a misplaced cfg attribute during installer module extraction. Restored the exact original function-level cfg boundaries and limited the root autostart import to its actual callers. Windows strict clippy passes; final Mac compilation, signed production and fresh-public installation passed.
