TT;DR: Deliver v1.1.0 as the first release with the shared goose control surface on Windows, macOS, and Linux. It begins only after the independently verified v1.0.3 hardening release, then adds shared, Windows, and Linux tray work before one exact-SHA atomic release.

## Why

The operator directly requested Windows and Linux tray parity after v1.0.2 established the macOS goose menu. The implementation must preserve the accepted macOS behavior and avoid three divergent settings or shutdown models.

## Scope

- Define one cross-platform tray contract and reusable asset pipeline.
- Implement and qualify native Windows and Linux control surfaces.
- Publish a complete all-platform v1.1.0 release through the existing exact-SHA atomic workflow.

Out of scope: native preferences windows, a second config schema, global quit keys, network control, weakening terminal protection, or claiming unsupported Linux desktop-shell behavior.

## Status

0/4 child tasks complete. All children are blocked until milestone #v103 publishes and verifies v1.0.3.

## Completed

## Activity

- 2026-07-17 04:45 — created from the operator's direct Windows/Linux tray and release order; linked existing #v1a and created separate shared, Windows, Linux, and release tasks. (agent: codex)
- 2026-07-17 04:50 — resequenced behind #r103 after the operator required two separate releases: hardening first, tray parity second. (agent: codex)
