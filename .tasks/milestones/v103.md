TT;DR: Complete the Alienware published-byte verification, make only evidence-backed Windows or cross-platform hardening changes, and publish them as immutable v1.0.3 before tray development begins.

## Why

The operator directly ordered two releases in this session. The first preserves the original verification-first plan as a focused hardening patch; the second independently delivers Windows/Linux tray parity. Separating them keeps hardware findings out of the feature release and gives tray development a known-good public baseline.

## Scope

- Run #v1a from fresh public v1.0.2 bytes on the Alienware.
- Reproduce and regression-test any finding before changing source.
- Include only compatible hardening/refinement work appropriate for a patch.
- Publish and independently verify immutable v1.0.3 through the exact-SHA atomic workflow.

Out of scope: Windows/Linux tray implementation, which begins only after v1.0.3 closes.

## Status

Done 2026-07-17. Both child tasks are complete; immutable v1.0.3 is stable/latest and independently
verified at `5192fab9690ff8b6777366a5918c12bbe1ee247a`.

## Completed

- #v1a — exact public bytes and native Alienware evidence completed; findings are regression-
  tested under ADR 0029 with unavailable/elevation boundaries recorded honestly.
- #r103 — candidate, same-SHA CI, atomic release, post-release smoke, public Windows bytes,
  supported upgrade, stable/latest, and live-site manifest verification completed.

## Activity

- 2026-07-17 07:20 — completed after both ordered hardening tasks and the immutable v1.0.3 public
  release passed; milestone #v110 is now active. (agent: codex)
- 2026-07-17 04:50 — created after the operator explicitly split the session into a v1.0.3 hardening release followed by a separate v1.1.0 tray feature release. (agent: codex)
