TT;DR: Implement and validate the visible Linux X11/XWayland backend: transparent overlay, input shaping/click-through, pointer/window support where X11 allows it, terminal filtering, and Linux x64/ARM evidence.

## Status

Open - the X11 visible backend and CI smoke script are in tree. This card closes after Linux x64
and ARM CI smoke passes and the run URL/evidence is recorded.

## Why

The Linux control-runtime foundation and X11 display backend are in-tree, but M17 readiness is
defined by Linux-host proof. This task owns visible X11 overlay and desktop-control evidence.

## Acceptance

- X11/XWayland mode renders the goose in a transparent always-on-top overlay.
- Input shape/click-through is limited to the goose hit region.
- Pointer sampling and cursor warp work where X11 permits them and report unsupported/failed otherwise.
- Foreign-window watch/ride filters terminal windows before emitting snapshots. Linux collect-window support remains unsupported and reports that state honestly.
- `honk300 status`, TUI Status, reload, stop, and direct pokes work during a visible X11 run.
- Cross-target checks still pass for Linux x64/ARM GNU and musl where installed.
- Linux-host CI smoke records window manager/session details and visible-pixel proof.

## Activity

- 2026-07-01 - added the X11 visible overlay path, input shaping, pointer sampling/warp, terminal-filtered foreign-window snapshots, and CI smoke under Xvfb/openbox; kept this card open pending CI evidence.
- 2026-07-01 - split from completed M16-M18 implementation card after `honk-platform-linux` and `src/runtime/linux.rs` landed with honest degraded status but no visible X11 overlay yet.
