TT;DR: Publish v1.3.7 as the immutable Windows updater fix-forward after v1.3.6 exposed a
quote-adjacent backslash transport failure before installation.

## Why
The fully published v1.3.6 release passed every hosted and public-byte lane, but the official
installed v1.3.5 `update --json` path found a live boundary those lanes did not execute. Its
generated PowerShell lease-extraction expression arrived as `Replace('','/')`, failed before
installer launch, and left the active v1.3.5 bytes and protected receipt unchanged.

## Scope
In scope: preserve the immutable v1.3.6 evidence; use transport-safe `[char]92` in the generated
archive path check; pin the exact generated form and execute it through Windows PowerShell
argument transport; publish v1.3.7 through the complete candidate/main/public process; finish the
official Windows install and isolated-config mud acceptance; close both release tasks on main.

Out of scope: changing archive traversal rules, lifecycle lease ownership, installer selection,
receipts, slot activation, mud tuning, configuration, platform packages, or published v1.3.6
assets.

## Acceptance
- The generated coordinator contains `[char]92`, contains no fragile `Replace('\','/')`, parses,
  and executes the normalization/containment probe through real Windows PowerShell.
- The complete local, candidate, main, publication, public-byte, and production gates pass.
- Official public Windows bytes install with exact receipt/provenance/runtime identity; isolated
  setup still materializes 10-second mud and leaves the real user config unchanged.
- v1.3.6 and its failure evidence remain immutable; final main is clean and all merged release
  branches are removed locally and remotely.

## Verification
- [x] Safe v1.3.6 failure and unchanged v1.3.5 receipt/install are recorded.
- [x] Focused generated-script and native PowerShell transport tests pass.
- [x] Complete local and committed-tree v1.3.7 gate passes.
- [ ] Exact candidate and unchanged-main CI pass before tagging.
- [ ] Atomic publication, all eight public lanes, and production identity pass.
- [ ] Official Windows installed/public-byte/config acceptance passes.

## Status
Active. The complete v1.3.7 Windows-host release gate passes, including 468 locked Rust tests,
127 Python contracts, strict native/cross-target checks, every parser, RustSec, cargo-dist,
release PE identity, and the 110-file package shape. Next: commit and verify the exact candidate,
then run PR and candidate qualification.

## Activity
- 2026-07-23 02:15 — created after official v1.3.5 self-update selected public v1.3.6 and failed
  before installer launch at `Replace('','/')`; v1.3.5 and protected receipt SHA-256
  `472ca1995ba4e2d8bebbf600f575f4b4d7620314f49bd0d8c2785f3db0da5c46` remained unchanged.
  The exact v1.3.6 bootstrap then reached genuine elevation, but consent was canceled. (agent:
  codex)
- 2026-07-23 02:22 — replaced both fragile quote-adjacent backslash uses in the generated
  coordinator with `[char]92`. The exact generated-script test prohibits the former expression
  while pinning replacement and containment, and a real Windows PowerShell `-Command` probe
  executes normalization plus rejection through the native argument boundary. Both focused tests
  pass; ADR 0040 and v1.3.7 release metadata/guidance are synchronized. (agent: codex)
- 2026-07-23 02:26 — complete local qualification passed: formatting/diff hygiene; strict native
  workspace plus x64/ARM64 musl Clippy; ARM64 Windows, Mac ARM64, and Linux ARM64 checks; 468
  locked Rust tests; 127 Python release contracts with three expected host skips; five PowerShell
  and nine shell parsers; actionlint 1.7.12; RustSec 0.22.2 over 418 dependencies; eight-target
  cargo-dist v1.3.7 planning; exact x64 console/GUI PE identity; optimized 1.3.7 binaries; and the
  unchanged 110-file package shape. (agent: codex)
