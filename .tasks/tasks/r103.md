TT;DR: Publish the Alienware-verified hardening work as immutable v1.0.3, independently verify its public Windows artifacts and update path, and finish on clean main before tray development starts.

## Why

The operator directly requested a first release for the original hardening verification plan, separate from the later tray release. This task converts #v1a evidence and only justified compatible fixes into a fully qualified patch.

## Plan

After #v1a closes, synchronize version 1.0.3 and durable documentation. Run the complete local and cross-target gate, then candidate mode on one exact SHA. Fast-forward main only after candidate success, wait for ordinary same-SHA CI, create the immutable v1.0.3 tag, publish atomically, pass post-release smoke, fresh-download public Windows artifacts, verify hashes/signatures/install/update/uninstall, validate stable/latest and the website, and clean temporary branches/worktrees.

## Impact

Intended: users receive the independently verified Windows hardening before tray behavior changes. Risks include accidentally mixing feature scope into the patch, weakening another platform's contracts, or publishing before native evidence agrees.

## Acceptance

v1.0.3 contains only reviewed patch-compatible changes, every supported artifact is built from the release SHA, all native/platform gates pass, public Windows bytes and update paths are independently verified on the Alienware, and the repository ends on clean main.

## Verification

- [x] Version, release metadata, readiness, board, guidance, and both changelogs are synchronized.
- [x] Complete local/cross-target/package/security/workflow and native Windows gates pass.
- [x] Candidate and ordinary main CI pass on the same exact SHA before tagging.
- [x] Atomic release, post-release smoke, fresh public Windows trust/hash/install/update/uninstall, stable/latest, and website checks pass.
- [x] Repository ends on clean main and #trayc becomes unblocked.

## Status

Done 2026-07-17. Immutable v1.0.3 is stable/latest at exact commit
`5192fab9690ff8b6777366a5918c12bbe1ee247a`; every exact-SHA, public-byte, and hosted native gate
passed with the physical Global elevation waiver retained explicitly. `#trayc` is unblocked.

## Activity

- 2026-07-17 07:20 — closed after candidate `29577145711` attempt 2, same-SHA CI `29577774029`,
  atomic publication `29578238463`, and post-release smoke `29578671930` passed. Fresh public
  immutable/latest/site manifests and all Windows x64 payload hashes agree; exact portable
  paired-DWM, public Corporate install/runtime/graceful-stop/self-uninstall, and supported public
  v1.0.2→v1.0.3 MSI migration passed. The release notes explain the required one-time installer
  rerun because immutable older Windows binaries cannot self-repair the updater parser defect.
  (agent: codex)
- 2026-07-17 07:25 — moved To-Do → Active after #v1a closure. Source version/docs and ADR 0029
  are synchronized; workspace fmt/Clippy/tests, audit, optimized x64 smoke and image inspection,
  local MSI assembly/payload identity, real optimized Corporate lifecycle, Windows ARM64, both
  Apple platform/core targets, all four Linux targets, dist, actionlint, scripts, and compatible
  packaging contracts pass. (agent: codex)
- 2026-07-17 06:55 — v1.0.3 candidate source now includes the guarded owned-receipt refresh and
  conservative Codex/VS Code protection under ADR 0029. Workspace Rust tests and 101 Windows-
  compatible Python contracts pass; three POSIX symlink/mode assertions remain correctly assigned
  to hosted Linux because this Windows host has no WSL distribution or unprivileged symlink right.
  (agent: codex)
- 2026-07-17 06:20 — accepted the reproduced Corporate MSI retry hardening: only the Corporate
  package moves to `afterInstallFinalize`, proven by forced-failure preservation, successful
  retry/uninstall, and empty sampled component-client enumeration. This remains patch-only scope
  for v1.0.3. (agent: codex)
- 2026-07-17 04:50 — created as the first of the operator's two explicitly ordered releases; tray work now depends on this public verification. (agent: codex)
