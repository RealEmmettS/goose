TT;DR: Publish an immutable v1.3.3 so the Windows app launcher can leave a breakaway-permitted
integrated-terminal job, allowing Codex's command runner to return while the goose remains alive.

## Why

v1.3.0 correctly separated the public controller, transient GUI launcher, and hidden runtime, but
its qualification waited on the short-lived origin shell process rather than the surrounding
Windows job. The Codex app places commands in a job with `JOB_OBJECT_LIMIT_BREAKAWAY_OK`; a new
process group stays in that job, so the tool cell continues tracking the otherwise detached goose.
The final v1.3.2 physical lifecycle exposed the gap after its installer/updater proof passed.

The black rectangles in clipboard screenshots were investigated independently. The exact public
presenter surface contained zero opaque-black pixels, ordinary and graceful app-launched
`CAPTUREBLT` frames were transparent, and the rectangle reproduced only through a screen-copy path
that omitted the required layered-window capture flag. Renderer bytes and DWM composition do not
need a product change.

## Plan

Have only the transient GUI launcher request `CREATE_BREAKAWAY_FROM_JOB` together with the existing
`CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP` runtime flags. If Windows returns access denied
because a managed job forbids breakaway, retry the original exact-sibling windowless launch. Keep
null handles, no shell, singleton/readiness/error cleanup, installer ownership, and every runtime
control contract unchanged. Freeze and publish one new exact commit, then install and prove it in
the same Codex job.

## Impact

Breakaway-capable integrated terminals release their command runner after readiness while the
runtime and tray stay alive. Managed jobs retain their explicit containment policy instead of
turning it into a product launch failure. The main risks are accidentally spawning twice after a
non-permission failure or weakening the no-console path; the one-error fallback and exact flag
contracts keep both bounded.

## Acceptance

One exact v1.3.3 commit passes the complete local, candidate, same-SHA main, atomic publication,
all-eight-public-byte, and production-site gates. The official Alienware install matches public
bytes and receipts. From the Codex job, the origin shell and tool call return while exactly one
transparent hidden runtime remains status-responsive, duplicate start preserves it, and graceful
plus forced stop leave no process.

## Status

Complete. Immutable v1.3.3 is public at exact source
`43b018473a105dd30910b6804a73edc240486c8e`, every hosted and production gate passed, the
official Global install is active on the Alienware, the no-op updater preserves its protected
receipt byte for byte, and an exact Codex start returns while the hidden runtime remains alive.

## Activity

- 2026-07-19 — reproduced the retained Codex job and queried its limits: the shell is in a job with
  `JOB_OBJECT_LIMIT_BREAKAWAY_OK` (`0x800`). Strict raw-presenter and `CAPTUREBLT` evidence proved
  the overlay remains transparent through the app launcher and graceful stop; non-`CAPTUREBLT`
  screen copy alone produced the pasted black rectangle. (agent: codex)
- 2026-07-19 — added breakaway-first runtime creation with access-denied-only fallback to the
  original windowless flags. Focused Rust and packaging tests pass. An optimized local build
  returned the exact Codex origin shell/tool call while its hidden runtime remained independently
  alive and status-responsive; a second command force-stopped it and no process remained.
  (agent: codex)
- 2026-07-19 — complete v1.3.3 local qualification passed: formatting/diff checks, 123 Python
  release contracts with three platform skips, five PowerShell parsers, actionlint, 446 Rust tests,
  strict native plus x64/ARM64 musl Clippy, optimized x64 console/GUI PE proof, ARM64 Windows
  release-mode compile check, RustSec over 418 dependencies, eight-target cargo-dist planning, and
  the 108-file package shape. The final 81.1-second physical matrix passed with public-shell
  detachment, all lifecycle aliases, and no obscuration using binary SHA-256
  `325E2E5A6B07C09E543C2BAB9D109C7FF950F7370D5D089ED4013960232F5660`; launcher hash is
  `22838ECF75AAB5B7CEFEBE14538A917F1377C56AF0CA2EE5389E0D4F7B97DD5A`. (agent: codex)
- 2026-07-19 — a separate exact Codex command returned while final runtime PID 36940 remained
  alive and status-responsive. Duplicate public start preserved PID 36940, graceful stop returned
  zero in 3.484 seconds, and zero product processes remained. (agent: codex)
- 2026-07-19 — clean committed source `0018ddd3fd3dcb6327e2377d42f55726c1a9710c` passed the
  exact 108-file package-list gate with the repaired launcher, both Windows entrypoints, bootstrap
  templates, ADR/readiness/task evidence, and v1.3.3 metadata. (agent: codex)
- 2026-07-19 — exact candidate `29713238162` and unchanged same-SHA main CI `29713577406` passed
  at final evidence commit `43b018473a105dd30910b6804a73edc240486c8e`. Atomic publication
  `29713944564` produced the signed/notarized Mac release, native Windows x64/ARM64 installers,
  Linux archives, both Debian packages, 47 public assets, and 22 manifest payloads; all-eight-lane
  fresh-public-byte run `29714354769` then passed. (agent: codex)
- 2026-07-19 — immutable and rolling-latest manifest SHA-256
  `6415D9AA36361BBC9AB4CC3DD4E2C6A664E031C4A2F1B1D50CF802E85DB21CAD` and Windows bootstrap
  SHA-256 `0D65E9EA98482B093CF79C63E388819D1F674A586D44A89C1665C4F2470CF4DB`
  matched byte for byte. Chrome verified the deployed v1.3.3 Windows, macOS, and Linux commands
  and exact review/checksum links with zero console errors; the 390x844 fallback viewport had no
  horizontal overflow. (agent: codex)
- 2026-07-19 — the exact public command succeeded with both processor environment variables
  removed and installed machine-scoped v1.3.3. The protected receipt pins the exact tag/commit and
  Global MSI SHA-256 `85EBD280CFB9336A900159917929C4C204A60EA66433E5BC32E4796191077B71`;
  the downloaded rolling-latest MSI matches. All aliases report v1.3.3 and share SHA-256
  `BFAF51F281D141725EB77C36793A5B1AFB2BDF400D3AB2E3A2BB195E937FB287`; the launcher matches its
  receipt at `2B9DAEA3BA644ADE31EFB6B7633372006D369635BE424DF683588B1B7BB880A3`.
  One HKLM registration remains. PATH owns one logical directory but retains two pre-existing
  textual spellings with and without the trailing slash; the retired LocalAppData program root is
  empty, and neither is claimed as cleaned. (agent: codex)
- 2026-07-19 — installed `goose update --json` exited zero with exactly one stdout object,
  `result=up_to_date`, and progress only on stderr; receipt bytes, SHA-256
  `A01B1EA186F88DC6D71991F41E1891126570B2A4EA1AAD560F7238499FEC55B0`, and timestamp were
  unchanged. Installed `goose start` returned in 5.2 seconds while runtime PID 49928 stayed alive;
  duplicate start preserved it, graceful stop completed in 3.486 seconds, and no process remained.
  The 80.3-second installed lifecycle/no-obscuration matrix passed every alias and lifecycle
  spelling. Grouped merged release branches v1.3.1-v1.3.3 were removed locally and remotely.
  (agent: codex)
