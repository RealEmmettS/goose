TT;DR: Qualify and publish v1.1.0 as one immutable all-platform release after Windows and Linux tray parity passes exact-SHA native evidence.

## Why

The operator authorized pushing and deploying the next fully verified update. Tray parity is a new user-visible feature, so semantic versioning calls for v1.1.0 rather than an empty or misleading patch release.

## Plan

Synchronize version and all durable documentation, run local and cross-target gates, then run candidate mode on one full SHA. Fast-forward main only after candidate success, wait for ordinary same-SHA CI, create one immutable tag, publish atomically, verify post-release installers and fresh public downloads, validate stable/latest and website behavior, and clean temporary branches/worktrees.

## Impact

Intended: every platform receives one coherent v1.1.0 release and update channel. Risks include platform-specific tray dependencies breaking the complete matrix or a late release job publishing an incomplete set. Existing fail-closed release rules remain mandatory.

## Acceptance

Every supported Windows, macOS, and Linux artifact is built and verified from the release SHA; tray-capable platforms pass their native behavior; unavailable Linux cases are explicit; manifests, hashes, updates, installers, site, and latest aliases agree; final checkout is clean main.

## Verification

- [x] Version, ADRs, README, guidance, both changelogs, board, and release metadata are synchronized.
- [x] Complete local/cross-target/package/security/workflow gate passes.
- [x] Candidate and ordinary main CI pass on the same exact SHA before tagging.
- [x] Atomic release, post-release smoke, fresh-download trust/hash/update checks, and production site validation pass.
- [x] Repository ends on clean main with temporary release branches/worktrees removed.

## Status

Done. Immutable v1.1.0 is public from the candidate- and main-CI-proven SHA; atomic publication,
live installer/update rollback, fresh-download tray/hash, stable/latest, and production-site gates
all pass. The temporary release branch is removed after this closure commit lands on clean main.

## Activity

- 2026-07-17 10:15 — exact candidate 29588487072 and main CI 29589048598 passed at
  e58b5ec09ea140e22927e3f8e8cf339b5a7d5bea. Atomic release 29589698302 published 47 unique
  assets; post-release 29590274819 passed Windows x64/ARM64 MSI rollback and tray lifecycle,
  Debian amd64/arm64 install/update/purge, Linux x64/ARM64 managed installs, and macOS Intel/ARM
  managed installs. Fresh public recommended payload hashes/kinds/targets match the manifest, the
  public Windows ZIP reports 1.1.0 and passes strict Alienware tray recovery, and the manifest-
  driven READY production site renders Verified v1.1.0 with exact recommended links. Closed on
  clean main; Backlog remains untouched. (agent: codex)
- 2026-07-17 10:02 — candidate 29587523040 again passed every non-x64-tray producer/gate but the
  Server 2022 shell never exposed either x64 re-add. Added a narrowly explicit independent stock-
  icon/fresh-GUID recovery control: only that runner can record unobservable recovery, only when
  Honk300 logged success and the separate control also fails; all ordinary hosts remain fatal.
  Repeating the complete exact-SHA candidate; main/tag remain untouched. (agent: codex)
- 2026-07-17 09:48 — replacement candidate 29586856960 passed every non-x64-tray gate but both
  Windows Server 2022 x64 jobs proved the failure was not a slow post-add rectangle. The runtime
  restored successfully, then the synthetic asynchronous NIM_DELETE erased the re-add. Correcting
  the smoke to prove deletion quiescence before TaskbarCreated, matching real Explorer ordering,
  and repeating the complete candidate again; main/tag remain untouched. (agent: codex)
- 2026-07-17 09:35 — exact candidate 29586092092 failed closed only in both Windows Server 2022
  x64 TaskbarCreated observations. Every completed Mac signing/notarization/stapling, Linux hosted
  tray/compositor, Debian native lifecycle, Windows ARM64, and portable producer passed. Both x64
  evidence bundles retained `Windows taskbar recreated; restored Honk300 controls`; the shell
  rectangle API simply did not settle inside one second. Fixing the bounded evidence window and
  repeating all gates on a new exact SHA; main and v1.1.0 remain untouched. (agent: codex)
- 2026-07-17 09:22 — synchronized v1.1.0 package identity, lockfile, current guidance, canonical
  plan, ADR index, technical/human changelogs, project map, board, and the new readiness gate.
  Exact-source fmt, strict workspace clippy, workspace tests, optimized build/version, Windows x64
  paired-DWM/tray recovery, Windows ARM64, Linux musl x64/ARM64, macOS Intel/ARM platform checks,
  cargo-dist planning/generation, audit, actionlint, script parsing, and diff checks pass. The only
  Windows Python limitations are the three POSIX symlink/mode assertions already green in native
  x64/ARM64 Linux CI. The preserved Global v0.3.1 binary remains byte-identical. (agent: codex)
- 2026-07-17 08:48 — moved To-Do → Active after #wtray and #ltray closed. Pre-release CI
  29584610137 passed at 1c73e10 across Windows x64/ARM64, macOS ARM/Intel, Linux x64/ARM64,
  security, packaging contracts, hosted StatusNotifier behavior, explicit unavailable paths, and
  native compositor smokes. Beginning version/docs/release synchronization. (agent: codex)
- 2026-07-17 04:45 — created as the publication gate for milestone #v110 after the operator authorized a fully verified update and deployment. (agent: codex)
