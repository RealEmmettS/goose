TT;DR: Ship the user-requested Goose desktop polish together in one update, proving the installed Windows entry points.

## Why
The user installed v1.10.1 and sees a tray icon but no recognizable application icon. Configure silently fails. Untouched leaf piles persist and look like dots; rubbing the goose no longer gives visible affection. App-menu naming should match the memorable goose command.

## Scope
One combined update: fix installed Windows Configure and verify tray Update; provide Windows app/search/installed-app icons; redesign procedural leaf silhouettes and expire untouched piles while preserving the existing walking/charging scatter physics; trigger hearts and a short friendly cursor-follow after about 2.5 seconds of rubbing, retaining a comfortable gap and never grabbing the pointer; use Goose as the app-menu display name across platforms and recommend goose as the terminal command. Preserve existing binary/package/receipt identities and compatible aliases, saved settings, opt-outs, autostart, media, immutable releases and terminal protection. Mac/Pi physical acceptance remains separate.

The native application is the primary interface. Opening Goose from an app menu or running the bare goose command opens its controls; explicit start commands and login startup retain the runtime route. CLI/TUI and the shared Rust services remain the secondary interface and backend, with no ownership redesign.

The user requests a set of distinct botanical SVG leaf designs, each recolored through the existing approved autumn palette. Maple, oak, birch and ginkgo must read as leaves at desktop size. Integrate their geometry with deterministic visual variation while retaining existing lifetime and charging physics. This is additional scope in the same update; Windows MSI icons and all macOS/Linux application icons and entry points remain required.

## Plan
Reproduce the installed failures before changing receipt or launcher code. Compare protected and per-user evidence, then test the corrected production route against stale, conflicting and tampered records. Inspect actual leaf/petting frames and exercise timed engine behavior. Complete one integrated local/native/package qualification cycle before publication. After two equivalent failures, stop repeating the attempt and capture the missing cause before continuing.

## Acceptance
The user's installed Configure opens the real settings window; the real tray Update helper visibly reports success/no-op/failure through the existing lifecycle owner. Windows search and installed-app entries display Goose and its icon. Leaves are recognizable at desktop scale, expire without contact and retain stronger running scatter. Sustained rubbing produces hearts and gentle following with a gap, a finite duration and no cursor mutation. Platform app-menu names and recommended command are Goose/goose while existing installation ownership stays valid.

## Verification
- [x] Production regressions reproduce and correct the installed receipt/launch failure without weakening verification.
- [ ] Exercise actual Windows tray Configure and Update, settings controls, icons and shortcuts from an installed package.
- [x] Test leaf expiry, speed-dependent scattering, sustained rubbing, follow distance/timeout/cancellation and absence of cursor commands.
- [x] Inspect the requested SVG leaf family and recolors, then the actual renderer's desktop-size piles and charging animation.
- [ ] Pass required local and exact-source native/package release checks, then fresh-public checks.
- [ ] Verify final Windows installation and coordinate the website's matching name/download guidance.

## Evidence
Initial installed probe: v1.10.1 machine-wide Global MSI. `honk300 settings` fails with `settings install receipt is invalid or conflicting`. The protected root receipt records MSI v1.10.1, while a per-user receipt still records PowerShell v1.3.5 for the same root. `update --check` reports the current stable version without error. Leaf source confirms untouched piles never expire and leaf rendering is a 3.5-by-2 ellipse. Existing kick velocity scales with goose speed and must remain intact.

## Status
Active. Installed v1.10.1 Configure/Update and native GUI Start/Stop are verified after preserving the stale receipt. The only saved preference change enables the user-requested petting feedback. The combined v1.11.0 source is implemented and under local/native/package qualification. Earlier v1.3.5 preservation records are historical.

## Activity
- 2026-09-09 — Native GTK resolves the installed application identity and exact approved icon while retaining one visible menu entry; the staged Debian update/restart passes. GNOME Debian ARM64 then exposes an atomic record-replacement race: observation stops safely but status remains failed instead of revoked. Reproduce it deterministically against production JavaScript, add one bounded fully validated consent-record reread, and preserve unsafe/repeated-change rejection. Record ADR 0053 and require fresh native qualification.
- 2026-09-09 — All five installer transitions now pass on native Windows x64 and ARM64. The Corporate MSI returns a cached icon under an environment-variable path; expanded native decoding and exact artwork comparison confirm the fix. Complete Linux running-window identity mapping after finding that the pinned GTK host stores but never applies its icon path. Add native GIO/GTK installed lookup, exact artwork comparison, and rollback coverage. The same candidate's Mac disk-image creation fails with Resource busy; qualify the complete updated source afresh.
- 2026-09-09 — All 33 source checks pass. The candidate proves actual Global MSI, PowerShell and Global EXE icons and GUI routes, then stops at Corporate MSI icon-path resolution. Expand environment-variable descriptors while retaining native decoding and exact artwork comparison; record the raw descriptor before verification and require a fresh native result.
- 2026-09-09 — Native Linux compilation catches a missed login-entry call after the explicit application-icon argument was added. Preserve the login entry's existing theme-icon choice, update the call and run the desktop-entry generation regression on Windows as well as Linux before fresh qualification.
- 2026-09-09 — User reports the generic taskbar icon and a Search result shown as Run command. Confirm the open window is still installed v1.10.1 with no native window/class icon, and the application index has no Goose entry. Retain both visible surfaces as explicit final installed acceptance checks. The new SVG-enabled binary passes the lifecycle-only smoke.
- 2026-09-09 — Review the four SVG masters, all sixteen approved-color variants and actual runtime piles, fade and charging frames. A bounded supersampled cache preserves smooth rotations and complete damage bounds. Required Rust checks, release build, all 141 Python checks and all four MSI schema validations pass; native package qualification remains pending.
- 2026-09-09 — User requests varied botanical SVG leaf shapes and repeated recolors, preserving the current palette, and explicitly keeps all platform application icons in scope. Generate four original Quiver SVG designs and sixteen recolors for review; integrate their geometry into the shared renderer.
- 2026-09-09 — The corrected MSI installs its real Start-menu link. The next candidate exposes a fixture assumption: MSI primary icons are queried through the documented ProductIcon API rather than requiring an EXE-style DisplayIcon registry value. Reproduce that distinction read-only on installed Microsoft components, and require the actual published MSI icon bytes to match the approved artwork.
- 2026-09-09 — Native MSI validation catches a scope conflict in the direct standard-directory substitution. Bind the resolved all-users path to an explicit machine-owned directory before costing, retain HKLM ownership and ICE43/ICE57 validation, and verify native path resolution locally without installing the development package.
- 2026-09-09 — The first real candidate installation identifies an undefined Global MSI menu directory: the link lands at the drive root. The user's original misplaced shortcut confirms the same defect. Correct to the standard all-users ProgramMenuFolder and retain native installed-link validation; record the unrelated Mac artifact-upload DNS failure separately.
- 2026-09-09 — The first hosted Linux lanes expose stale expected menu labels in the private-D-Bus test. Align exact labels and remaining native descriptions with Goose, preserving real protocol dispatch and watcher recovery, and qualify the corrected source before publication.
- 2026-09-09 — Complete local source checks and actual leaf/affection contact-sheet review. All three staged Windows launch routes expose loaded native controls and the Goose window icon using an isolated profile. Preserve the expected refusal of unmanaged development autostart, and keep the production launch test within its established startup deadline. Record pending release gates in v1.11.0 readiness.
- 2026-09-09 — User requests one combined update for Windows icons/Configure/Update, temporary recognizable leaves with the strong running scatter preserved, affectionate follow after rubbing, and Goose/goose naming. Reproduce installed Configure failure and identify contradictory protected/per-user receipt origins. Begin bounded repair and visual/behavior qualification.
- 2026-09-09 — User makes the application primary and CLI/TUI secondary without a backend redesign. Add ADR 0052, correct current-owned receipt precedence, restore installed Configure, verify real native tray actions and GUI lifecycle controls, inspect procedural leaf frames, and pass the new timed affection and cancellation tests. Native settings tests/check/build and OS UIA interaction/save/readback pass locally. Add production installed-entry regression coverage, including stale receipt and visible icons, to both native Windows installer lanes.
