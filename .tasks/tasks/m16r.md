TT;DR: Run the macOS-host readiness smoke for the M16 backend on real macOS hardware, including universal2 bundle validation, Accessibility denied/granted behavior, terminal protection, status/reload/stop, audio, collect note/meme, and Intel/Apple Silicon evidence.

## Status

Active under `#m20q`. Hosted macOS bundle/status CI smoke passed on arm64 and Intel hosted
runners. On 2026-07-12 this physical M2 ran the denied smoke and then, without changing the signed
Developer ID executable, the operator granted the visible Honk300 Accessibility entry. Status
reported Accessibility, cursor, and window support and the complete granted script passed honk,
mud, reload, nab, meme, note, and stop. The one-display live multi-monitor/hot-plug limitation is
now explicitly waived while automated topology coverage remains mandatory and green. Final
closure still requires the exact candidate's ADR 0022 four-state sequence plus an ordinary-window
positive and protected-terminal negatives; that evidence must be recorded without claiming a
live multi-monitor run.

## Why

The M16 implementation is in-tree and cross-target checks pass from Windows, but macOS-specific proof cannot be produced on a Windows host. This task owns the host evidence required before claiming full macOS backend readiness beyond implementation closure.

## Plan

Use `script/smoke_m16_macos_accessibility.sh` and ADR 0022's managed-app flow rather than a
pre-granted-only shortcut. Build, sign, notarize, and install one exact candidate; record its full
commit and executable digest once, and do not rebuild or replace it anywhere in the sequence.
Native prompt/no-prompt observations require the awake operator. The script's reset and cleanup
flags are explicit and scoped only to Honk300.

Resolution steps:

1. With explicit reset opt-ins, start the exact managed candidate denied. Confirm one native
   consent request and Settings handoff, owner-only marker creation, calm safe-edge wait, truthful
   denied status, working status/reload/honk/stop, and `BUSY` for prank actions.
2. Stop and relaunch the same denied candidate. Recheck its digest and marker identity, then
   confirm neither consent UI nor Settings reopens.
3. Grant Accessibility while that same process is running. Record the transition time,
   same-process FirstUX resume, supported Accessibility/cursor/window status, ordinary-window
   positive, collect/nab behavior, and protected-terminal negatives.
4. Revoke Accessibility while that same process is running. Record the transition time, denied
   status, cancelled permission-bound work, return to the safe wait, no new prompt, and working
   status/reload/honk/stop.
5. Record the one-display hardware waiver without claiming live multi-monitor/hot-plug, then use
   the separate explicit cleanup opt-in to purge the managed test install and reset only
   Honk300's marker/Accessibility record where macOS permits.
6. Append the digest, transcripts, observations, transition times, and cleanup result to
   `docs/readiness/m16-m18-readiness.md`. Close this task only after every verification item is
   passed or explicitly waived with a reason.

## Acceptance

- `script/smoke_m16_macos.sh` passes on a macOS host.
- `script/package_macos_app.sh` produces `Honk300.app` and validates `plutil`, `codesign`, and `lipo -verify_arch x86_64 arm64`.
- Bundled app launches through `/usr/bin/open -n ... --args start --config <path>`.
- Without Accessibility, overlay/wander/status/reload/stop/audio degrade or work as expected and cursor/window features report denied/unsupported without crashing.
- The exact candidate passes ADR 0022's first-denied, non-nagging denied relaunch, live-grant,
  and live-revocation states without a rebuild or process restart between live transitions.
- With Accessibility, cursor nab, foreign-window ride, collect note, collect meme, terminal
  non-targeting, TUI reload/status, and `honk300 stop` are manually smoke-tested; multi-monitor is
  tested live only where hardware permits and otherwise explicitly waived.
- Evidence covers arm64 native and x86_64 via Rosetta or Intel hardware.
- CI run URL, app artifact names, and Accessibility-granted evidence are recorded in `docs/readiness/m16-m18-readiness.md`.

## Verification

- [x] Latest GitHub Actions state checked for newer macOS Accessibility evidence.
- [x] Hosted macOS bundle/status smoke evidence exists for arm64 and Intel hosted runners.
- [x] Accessibility-granted smoke passes on a pre-granted self-hosted/manual macOS host.
- [x] Evidence includes `accessibility: supported` and `cursor: supported` status output.
- [x] Evidence covers `honk`, `mud`, `reload`, `nab`, `meme`, `note`, and `stop`.
- [ ] Exact candidate first-denied state records one prompt/Settings handoff, marker, safe wait,
  denied status, allowed controls, and busy prank rejection on one binary digest.
- [ ] Same denied candidate relaunch preserves digest/marker and does not reopen consent UI or Settings.
- [ ] Live grant on the unchanged running candidate records transition time, same-process FirstUX,
  supported capabilities, and ordinary-window positive behavior.
- [ ] Live revocation on that same process records transition time, denied capabilities, cancelled
  permission work, safe wait, no new prompt, and working controls.
- [ ] Terminal non-targeting observations are recorded on the exact signed candidate.
- [~] Live multi-monitor/hot-plug is hardware-waived on this one-display Mac; automated signed-
  coordinate/topology/hot-plug coverage passes, and no live validation is claimed.
- [ ] `docs/readiness/m16-m18-readiness.md` records the final four-state exact-candidate evidence.

## Activity

- 2026-07-14 01:25 - Mac stopping-point code, packaging, lifecycle, and local gates are green, but
  this card remains Active because no notarized exact candidate was frozen. The next Mac run must
  use one candidate digest for first-denied, non-nagging relaunch, live grant, live revocation,
  protected-terminal negatives, and an ordinary-window positive (agent: codex).
- 2026-07-13 23:22 - replaced the obsolete pre-granted-only plan with ADR 0022's exact-candidate
  four-state denied/non-nag/live-grant/live-revocation sequence, explicit operator observations,
  scoped cleanup, and unchanged-digest requirement (agent: codex).
- 2026-07-13 01:10 - recorded the physical Mac's one-display limitation as an explicit hardware
  waiver. Automated signed-coordinate, topology, per-display-window, and hot-plug reconciliation
  tests pass; live multi-monitor validation is not claimed (agent: codex).
- 2026-07-13 01:08 - ADR 0022 onboarding implementation and smoke contracts are automated-green.
  Kept the card open because changing the macOS Accessibility security toggle requires the awake
  operator at action time and the exact candidate four-state/protected-terminal evidence remains
  a hard gate (agent: codex).
- 2026-07-12 21:12 - physical M2 evidence superseded the earlier host blocker: one unchanged
  Developer ID-signed app passed denied mode, then reported Accessibility/cursor/window support
  after the operator grant and passed honk, mud, reload, nab, meme, note, stop, and 60 immediate
  stop/start cycles. Kept the card open for final-candidate terminal observations and the
  single-display waiver (agent: codex).
- 2026-07-10 19:14 - v0.3.1 readiness closure rechecked CI run `29131779321`: Intel and Apple
  Silicon hosted bundle smoke passed; the opt-in pre-granted Accessibility job was correctly
  skipped. Kept this card open and documented as nonblocking; no grant-persistence claim was made.
- 2026-07-07 - user explicitly chose to continue M19 first and defer macOS Accessibility. Kept this
  card open with its evidence checklist intact; next action remains waiting for a pre-granted Mac
  or self-hosted runner.
- 2026-07-06 22:23 - investigated active-task plan; latest successful CI run `28569889803`
  still skipped `macOS Accessibility smoke`, so the task remains blocked on pre-granted
  self-hosted/manual macOS evidence. Added a board-visible resolution checklist and linked the
  planning canvas at `docs/thinking/2026-07-06-active-task-resolution-plan.md`.
- 2026-07-02 - hosted macOS bundle/status CI passed in
  <https://github.com/RealEmmettS/goose/actions/runs/28569332035>; artifacts:
  `honk300-macos-macos-15` and `honk300-macos-macos-15-intel`. Kept this card open because
  macOS Accessibility smoke job
  <https://github.com/RealEmmettS/goose/actions/runs/28569332035/job/84703318760> was skipped,
  so cursor/window/collect behavior with granted Accessibility was not yet proven.
- 2026-07-01 - added `.github/workflows/ci.yml`, fixed the hosted macOS app path smoke, added `script/smoke_m16_macos_accessibility.sh`, and kept this card open pending real CI/manual Accessibility evidence.
- 2026-07-01 - split from completed M16-M18 implementation card because Windows can compile/check macOS targets but cannot produce AppKit, Accessibility, universal2, or codesign runtime evidence.
