TT;DR: Publish the v1.2.6 Windows compatibility fix-forward so the versionless official CLI bootstrap is fully public-byte-qualified on Windows, macOS, and Linux, while updates preserve the proven owner and deliberate fresh official installers safely become the new owner.

## Why
The sibling TR-300, ND-300, and SD-300 updater tasks converged on a shared lifecycle rule: command-first installation is only honest when the official bootstrap is a managed channel, not merely documentation around an unmanaged package-manager command. Goose already has managed PowerShell and shell bootstraps, but the recommendation is package-first and the Mac shell template currently preserves an old DMG receipt even when the user deliberately launches a fresh shell install.

## Plan
- Add ADR 0034, superseding only the prior package-first recommendation while retaining DMG/MSI/EXE/DEB artifacts and every provenance-preserving update rule.
- Advertise the stable `irm ... | iex` command on Windows and `curl ... | sh` on macOS/Linux; describe native packages as explicit alternatives and raw Cargo/source installs as unmanaged development paths.
- Pass trusted updater intent into the Mac bootstrap so a DMG-owned `honk300 update` keeps the DMG channel, while a fresh public shell install commits shell ownership after verification.
- Audit and test recognized-channel takeover in both directions for PowerShell versus Windows native installers, shell versus DMG on Mac, and shell versus Debian on Linux. Fail visibly where scope or ownership makes safe convergence impossible.
- Preserve immutable v1.2.3–v1.2.5, bump the compatibility fix-forward to v1.2.6, update technical and human changelogs, run the complete noninteractive gate, and use disposable platform-native CI for installer mutation and GUI-app qualification.
- Publish one immutable exact-SHA patch, verify public bytes/latest aliases/manifest, then update and deploy the production website copy.

## Impact
Intended: most users get one copyable versionless command and later `honk300 update` behaves predictably. Users choosing DMG, MSI, EXE, or DEB retain those formats across updates. A later fresh official installer can intentionally change channels without leaving two active owners.

Risks: a transport helper must not accidentally rewrite provenance during an update; cross-scope Windows or Linux cleanup must not claim success without authority; old signed releases and assets must remain immutable; raw Cargo must not be misrepresented as a supported takeover mechanism.

## Acceptance
Focused provenance/takeover tests pass; the complete local and exact-candidate matrices pass; the same SHA reaches main, tag, atomic release, post-release public-byte qualification, stable/latest aliases, and the production site; only main remains locally and remotely; the board records exact evidence.

## Status
Done. v1.2.6 is public stable/latest from exact source `c0ddb2c2c7cd4334040e324c72187eb6f3d4a644`; candidate, same-SHA main, atomic publication, all eight fresh-public-byte lanes, latest aliases, production website, and temporary-branch cleanup are complete.

## Activity
- 2026-07-18 — re-read the active TR-300, ND-300, and SD-300 tasks. All agree that official bootstraps are managed authoritative installers, native packages remain alternatives, raw Cargo is unmanaged, updates preserve the durable owner, and a deliberate fresh official install must converge recognized prior ownership or fail visibly.
- 2026-07-18 — confirmed Goose keeps its signed/notarized DMG as the graphical installer by design; the sibling diagnostic tools' direct-PKG migration and legacy-DMG bridge are not applicable to the Goose app-bundle experience.
- 2026-07-18 — refreshed the sibling tasks again. ND-300 proved direct PKG plus legacy-DMG compatibility on both native Mac architectures; TR-300 sharpened the shared rule that stable public wrappers own discovery while immutable exact-tag tools own mutation, and native opposite-scope transactions must never leave stale registration while claiming success.
- 2026-07-18 — completed the critical-thinking/logical-reasoning design audit. The load-bearing conclusion holds only if recommendation, receipt, authority, rollback, and final public-command ownership agree. ADR 0034 and the implementation retain Goose's transactionally safe Windows cleanup-pending model, add pre-commit Linux scope refusal, and distinguish fresh Mac intent from updater-only transport hints.
- 2026-07-18 — synchronized source version 1.2.3, ADR/canonical plan, README/project guidance, both changelogs, readiness tracking, Mac Accessibility eligibility, Windows origin behavior, and Linux collision preflights. Focused and full qualification remain pending.
- 2026-07-18 — compatibility audit caught an immutable-old-client edge before release: v1.2.2-and-earlier Mac updaters cannot set the new origin hint. Added a receipt-validated bridge limited to their established `honk300-update-*-honk300-installer.sh` temp name and extended signed/public smoke to prove both old and new transports preserve DMG ownership while normal fresh installs retake shell ownership.
- 2026-07-18 — complete noninteractive pre-candidate gate passed: strict native/both-musl Clippy, 434 Rust tests, locked release build, ARM64 Windows check, 120 Python contracts with three honest host skips, shell/PowerShell parsers, actionlint, cargo-dist v1.2.3 plan, RustSec scan of 410 dependencies, exact x64 PE subsystem checks, diff hygiene, and the clean committed 104-file package list. No Goose, installer, UAC prompt, or visible helper ran locally.
- 2026-07-18 — exact v1.2.3 candidate `29659033819` and same-SHA main CI `29659567428` passed at `f85aab4b9e5b539e2a853d7f3451ed9a560b07dc`. Atomic run `29659905452` failed before draft creation on three attempts because its hosted Windows shell never exposed the exact x64 tray rectangle. The tag remains immutable and unpublished.
- 2026-07-18 — refreshed the sibling tasks once more. SD-300 adds one-item installer-query normalization and new-owner completion proof when Restart Manager ends the parent; TR-300 adds transactional quarantine/restore of the managed binary plus receipt. Goose's slot transaction already follows the same ownership principle.
- 2026-07-18 — opened the v1.2.4 fix-forward under ADR 0035. Its hosted x64 fallback requires the exact Honk300 owner plus a matching failure from an independent fixed-GUID registration/recovery probe, records explicit evidence, and still runs native tray-menu and lifecycle tests. Ordinary Windows CI remains strict and unwaived.
- 2026-07-18 — complete v1.2.4 noninteractive local gate passed: format, strict native/both-musl Clippy, ARM64 Windows check, 434 Rust tests, locked release build, 120 Python contracts, all script parsers, actionlint, cargo-dist plan, 410-dependency RustSec audit, diff hygiene, and package-shape review. No visible desktop process ran.
- 2026-07-18 — v1.2.4 candidate `29660728786`, same-SHA main `29661048121`, and atomic publication `29661340294` passed at `03ae458a880c6b0faf1c1cf96fb28a69ac2936f2`; 47 assets are public and exact/latest manifests plus bootstraps are byte-identical.
- 2026-07-18 — public-byte run `29661686919` installed and trust-verified the Mac app twice on both architectures, then rejected the valid DMG takeover receipt because the harness required only the shell writer. Opened v1.2.5 to accept exactly the managed shell/native-app writers while retaining disabled-autostart and foreign-LaunchAgent proof.
- 2026-07-18 — refreshed SD-300, TR-300, and ND-300 once more. Their latest Windows evidence reinforces that installer inventory must remain array-shaped and that a completed new owner, not a parent process alone, is authoritative when Restart Manager participates. Goose's public Windows failure had a narrower harness cause: it occupied the exclusive lifecycle lease before invoking the bootstrap that must acquire it. v1.2.5 separates that public-bootstrap proof from the existing x64/ARM64 live-old-slot proof and records subprocess output.
- 2026-07-18 — complete noninteractive v1.2.5 local qualification passed: format/diff hygiene, strict native/both-musl Clippy, ARM64 Windows check, 434 Rust tests, locked optimized build, all 120 Python contracts, every script parser, actionlint, cargo-dist planning, RustSec, and package shape. The focused public Mac receipt and Windows bootstrap contracts were repeated after the final harness fix; no product, installer, UAC prompt, or visible helper ran locally.
- 2026-07-18 — v1.2.5 candidate `29662126045`, same-SHA main CI `29662448054`, and atomic publication `29662746481` passed at `76e970a0242e49617528b69ec87202bbc5171854`; exact/latest manifest and bootstrap bytes match across 47 immutable assets. Public run `29663081572` passed both Mac, both shell-Linux, and both Debian lanes. Captured Windows x64/ARM64 stderr proved the public bootstrap's MSI completed before installed-launcher verification failed because system Windows PowerShell lacked `Get-FileHash`.
- 2026-07-18 — opened v1.2.6 and removed the optional cmdlet dependency from both bootstrap launcher verification and the CLI coordinator's owned-temp cleanup. Both paths now use regular-file/reparse-checked .NET file streams plus SHA-256, matching their existing pinned download verifier.
- 2026-07-18 — complete noninteractive v1.2.6 local qualification passed: format/diff hygiene, strict native/both-musl Clippy, ARM64 Windows check, 434 Rust tests, locked build, 120 Python contracts, all script parsers, actionlint, cargo-dist planning, RustSec, and package shape. No product, installer, UAC prompt, or visible helper ran locally.
- 2026-07-18 — refreshed ND-300, SD-300, and TR-300 one final time. ND-300's real Global-MSI update and SD-300's completed CLI-first implementation matched Goose's ownership contract. TR-300's new rule to prove package takeover before payload mutation is already satisfied by Goose's verify-before-activation DMG/app-ZIP and immutable-slot transactions; its direct-PKG mechanics do not apply to Goose's retained graphical DMG.
- 2026-07-18 — v1.2.6 candidate `29663447227`, same-SHA main CI `29663774278`, and atomic publication `29664104522` passed at exact source `c0ddb2c2c7cd4334040e324c72187eb6f3d4a644`. The immutable release contains 47 assets and 22 manifest payloads.
- 2026-07-18 — independently downloaded exact/latest manifest, PowerShell bootstrap, and shell bootstrap bytes matched at SHA-256 `3345862c98bb043d3d6940ab9f82822defb22147af874bdb320b3bdcde95beb9`, `868466b1e796261f3d546f0ea7ce6293a0380f7965f927b5a146797f4ff8d4e5`, and `6db94c80a4bc65dbe693be9081d2db304b2e7f24359a617511751f7c7663cc0a`.
- 2026-07-18 — post-release run `29664433689` passed all eight fresh-public-byte jobs, including the actual released PowerShell bootstrap and Global-MSI takeover on Windows x64/native ARM64 plus both Mac, shell-Linux, and Debian architectures.
- 2026-07-18 — production `thegoose.app` browser QA showed verified v1.2.6, correct command-first Windows/Mac/Linux presentations and native alternatives, no console warnings/errors, and no horizontal overflow at a 390x844 mobile viewport.
- 2026-07-18 — every temporary Goose/site branch was proven merged and removed locally/remotely. Goose retains only main. The site retains clean main plus the unrelated open unmerged ImgBot PR branch, which was deliberately preserved outside this release.
