TT;DR: Improve settings navigation and scrolling, hide unsupported Wayland controls, and align tray/menu artwork with the application logo. Ship the changes together as the next immutable update.

## Why
The user verified that the latest Windows app now has its logo and opens settings successfully. They report indistinguishable selected tabs, a wheel step that runs to the bottom, and a Wayland switch on Windows. They also request consistent tray/menu and application artwork.

## Scope
Keep the existing settings layout, shared Rust configuration service, saved preferences, runtime behavior, installer ownership and approved application artwork. Change navigation styling, scroll behavior, platform visibility and control-surface icon presentation. Preserve the Mac template adaptation where required for native menu-bar appearance.

## Plan
Reproduce the installed behavior, correct the existing presentation/configuration seams, and qualify real native interaction before the complete release gates. Version 1.11.1 is available for this update.

## Acceptance
The selected page remains visually distinct after hover/focus moves. A wheel step makes a small smooth movement and stops; repeated/reversed input, boundaries and reduced motion remain predictable. Unsupported platforms do not show the Wayland switch. Application and system-control artwork are consistent and readable at native sizes. Publish only after the same-source release and public-distribution gates pass.

## Evidence
- User confirms the installed Windows logo and settings launch work; the installed command and running immutable slot report 1.11.0.
- Native Windows settings window opens from the current installed path and displays the Goose icon and version.
- The selected ghost button loses its distinguishing fill away from hover. The SDK gives one 40-unit Windows wheel step a velocity of 2400 units/second with a 0.86 per-second decay.
- The rebuilt v1.11.1 window passes all five selected-tab appearances, single-notch settling, reversal and Windows Wayland absence. The isolated file retains its original hash. SDK tests/check/build, Rust fmt/clippy/workspace tests/release build and 141 Python checks pass locally (three host-specific skips).
- The existing lifecycle-only Windows test passes with an actual registered/recovered tray. The installed v1.11.0 runtime is restored and its current saved configuration hash is unchanged. Logs and native state evidence are under `target/settings-polish-review/`.

## Verification
- [x] Native selected-tab, scroll and platform-control behavior passes with an isolated profile.
- [x] Scroll checks cover bounded settling, repeated/reversed input, boundaries and reduced motion.
- [ ] Each platform's tray or menu icon matches its existing application artwork; Mac template identity and native decoding remain correct.
- [ ] Required local checks and exact-source CI, candidate and unchanged-main gates pass.
- [ ] Complete immutable release and all fresh-public lanes pass; public bytes and website guidance are verified.

## Status
Active. Native settings behavior passes locally. Qualify native tray behavior, then the complete same-source release and public download gates.

## Activity
- 2026-09-09 — Correct selected-tab styling, bound scroll momentum and filter Wayland by platform. Inspect all five rebuilt native pages and a single wheel step plus its settled/reversed state with an isolated profile. Retain the hidden Wayland value. Load Windows tray artwork from its actual application resource; use the existing Linux/Mac application PNG for their control surfaces. Preserve the user's approved application art.
- 2026-09-09 — User adds tray/menu icon consistency to the settings fixes and authorizes one new release. Inspect the installed 1.11.0 window, SDK scroll physics and shared control-surface icon paths.
