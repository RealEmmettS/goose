TT;DR: Publish the idempotent Windows update-discovery repair as immutable v1.3.1, verify every supported platform from one exact SHA, then install the official release on the Alienware and prove both updating and the detached app runtime.

## Why

Public v1.2.6 reproduced `PermissionDenied` before download from a normal administrator terminal.
The same pre-download code is present in public v1.3.0: compatible PowerShell/Global-MSI ownership
has no conflict, but cleanup discovery still replaces the unchanged receipt under Program Files.
Because the failure precedes download and elevation, neither published binary can self-repair.

## Plan

Release a patch from the reviewed idempotent receipt-state fix. Preserve the proven origin,
immutable Windows slots, existing UAC installer boundary, conflicting-owner journal, exact artifact
checks, and v1.3.0 controller to app launcher to hidden runtime contract. Freeze one candidate SHA,
pass all platform producers and consumers, fast-forward it unchanged to main, wait for ordinary CI,
publish a new immutable tag, and verify all public bytes plus the production site. Finally cross the
broken installed updater with the official versionless PowerShell bootstrap and prove subsequent
`goose update --json` succeeds normally.

## Impact

Intended: ordinary Windows terminals can check for updates without writing protected state, and UAC
appears only when verified installation work requires it. Risks include weakening owner cleanup,
publishing a platform-incomplete release, installing non-public bytes, or regressing the new detached
app launcher. Older tags and assets remain untouched.

## Acceptance

One exact v1.3.1 commit passes local, candidate, same-SHA main, atomic publication, and eight-lane
fresh-public-byte gates. Exact-tag/latest manifests and versionless installers agree. The physical
install boundary is exercised without mutating the old install; if it exposes a separate bootstrap
defect, immutable v1.3.1 remains published and the install/updater/runtime proof moves to a new
fix-forward task.

## Status

Done for stable v1.3.1. The release passed every hosted and public gate. The first physical install
attempt then exposed a separate scrubbed-environment bootstrap defect before download or UAC, so
v1.3.1 remains immutable and official Alienware install proof continues under `#r132`/v1.3.2.

## Activity

- 2026-07-19 — operator authorized full publication and installation. Reconstructed clean matching
  `main`/`origin/main` at `bd8596c`, confirmed v1.3.1 has no local/remote tag or GitHub release, and
  opened `codex/release-v1.3.1` with only the updater repair plus its documentation. Selected a patch
  because behavior and interfaces remain unchanged; public v1.2.6/v1.3.0 stay immutable. (agent: codex)
- 2026-07-19 — complete working-tree qualification passed: strict native and both musl Clippy
  targets, 445 Rust tests, x64 optimized build, ARM64 Windows release check, 122 Python contracts,
  all PowerShell/action workflows, RustSec, eight-target cargo-dist v1.3.1 planning, and 108 packaged
  files. The 79-second physical x64 lifecycle also passed the detached controller/app/runtime,
  singleton/tray, graceful stop, and immediate-restart contract without full-screen calibration;
  no Honk300 process remained. (agent: codex)
- 2026-07-19 — committed source `fb57f177729df0399a0c92a6f4e20b24443889eb` passed the clean
  108-file package-list gate with the updater repair, app-launcher resource, bootstrap template,
  and v1.3.1 metadata present. Recorded the proof before freezing the final candidate SHA.
  (agent: codex)
- 2026-07-19 — froze exact candidate `682e674e0e2481a18cc2f39fa474962163d5ad74` and passed candidate
  `29707311920`, unchanged-main `29707620354`, atomic publication `29707897998` attempt 2, and all
  eight fresh-public lanes in `29708391434`. v1.3.1 has 47 assets and 22 manifest payloads; exact
  tag/latest manifest and installer bytes match. The first publication attempt failed closed before
  release creation on transient macOS `hdiutil: Resource busy`; rerunning the unchanged tag passed.
  (agent: codex)
- 2026-07-19 — production showed v1.3.1 and the correct versionless commands for Windows, macOS,
  and Linux with a clean browser console and no horizontal overflow at 390x844. The first exact
  Windows command from the Codex app terminal then failed before download/UAC because that host
  omits both inherited processor variables. The v1.2.6 receipt hash/timestamp and installed files
  were unchanged. Opened `#r132` for the narrow immutable fix-forward and transferred physical
  install/updater/runtime acceptance there. (agent: codex)
