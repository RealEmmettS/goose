# Rust build artifacts
/target
/inno/Output/

# Cargo.lock IS committed (this is an application workspace, not a library) — it pins
# the dependency graph for reproducible builds and `cargo audit` (plan §15, risk O_supply).

# Editor / OS cruft
*.rs.bk
.DS_Store
Thumbs.db
__pycache__/
*.py[cod]

# Personal Claude Code settings (local hooks, etc.) — never committed
/.claude/settings.local.json

# Transient Claude Code agent worktrees — never committed
/.claude/worktrees/

# Playwright MCP session artifacts — never committed
/.playwright-mcp/
